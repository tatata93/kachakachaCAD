# kachakachaCAD 改修報告書

2026-09-06 / 対象コミット `11c49e8`（GitHub main）
自宅PCで **ビルド成功・全18テスト合格・起動確認済み**（OCCT 8.0.1 / Qt 6.9.2 / MSVC 2022）

---

## 0. ご指示と、やったこと

| ご指示 | 状態 | どこで確認できるか |
| --- | --- | --- |
| ①編集するグループをあらかじめ指定し、分類しなおす手間をなくす | 完了 | ツールバーの「グループ」欄、モデルツリーの「◀作業中」 |
| ②作る対象をチェックボックスで選ぶ／押し出しへ統合 | 完了 | 面・板材モード →「押し出し・厚み化」 |
| ③近似部品1・2・3のように面を割り当てる | 完了 | 近似モデルモード →「部材にする」の表の「部品」欄 |
| 面へ投影は残す | 残しました | 面・板材モード →「面へ投影」 |
| ④出力モードの刷新（表・別kcd/STL/STEP・3D確認・自動でふさぐ） | 完了 | 出力モード →「3Dモデル・プロジェクト」 |
| ⑤全機能を網羅的に試験し、問題が無くなるまで直す | 完了（下記5章） | ctest 18本 + 自動UI試験 + 実機スクリーンショット15枚 |
| ⑥ChatGPTと相談して、もっと分かりやすいUIにする | 完了（下記6章） | Codex/gpt-5 のレビューを反映 |
| ⑦Windows 95 風UI（通常版と選べる） | 完了（下記7章） | 設定メニュー →「Windows 95 風の見た目」 |
| 報告書 | 本書 | |

「ユニットを近似を押しても近似されない」件は、**原因を潰したうえで、
同じことが起きても理由が必ず画面に残るようにしました**（3章）。

---

## 1. 作業中グループ

**問題**: 前面グループを作って線を移しても、次に引いた線はまた未分類に入る。

**直し方**: プロジェクトに「作業中グループ」を持たせ、**新しく作る物すべて**
（作業平面・点・線・面・板材・治具・投影線・オフセット線）を自動でそこへ入れます。

- ツールバーに「グループ」の選択欄を足しました。ここで選んだ物が作業中です。
- モデルツリーでは、作業中のグループが**開いたフォルダの絵と「◀作業中」**で分かります。
- .kcd にも保存されるので、ファイルを開き直しても作業中のままです。
- **注意して作ってある点**: 近似モデルの再計算などで内部的に作り直される派生物は、
  作業中グループに吸い込まれません（吸い込むと、作業中を切り替えるたびに
  既存の部材の所属が壊れるため）。この動きは自動試験で守っています。

## 2. 押し出しへの統合

**考え方**: 「押し出し」は、線や面を決めた方向へ決めた距離だけ伸ばす一般形です。
「面へ厚み」は距離を面までにしただけ、「厚み化」は面を法線方向へ伸ばしただけ。
そこで**同じ道具にまとめ**、道具列から「厚み化」を無くしました
（道具名は「押し出し・厚み化」）。

画面の先頭で目的を選びます。

- **線・面を伸ばす** … 方向（面の法線＝自動 / ±X・±Y・±Z）と距離、または「選んだ面まで」
- **面に厚みを付ける** … 板厚・厚み方向・材質・可変厚み（始端と終端で厚みを変える）

「線・面を伸ばす」で**作る物をチェックで選べます**（ご指示の「チェックボックスで選ぶ」）。

| チェック | できる物 | ご指示との対応 |
| --- | --- | --- |
| 先端のワイヤ | 伸ばした先の輪郭線 | オフセット面／線だけ、の「先端」 |
| 押し出し方向の稜線（線だけ） | 角ごとに、元から先端へ伸びる線 | 「押し出した距離方向も含めたワイヤ」 |
| 側面（押し出した面） | 元の輪郭と先端の間に張る面 | |
| 先端のふた面（閉じた輪郭のみ） | 先端側を塞ぐ面 | 「全面面付き」 |
| 元の位置のふた面（閉じた輪郭のみ） | 元の位置側を塞ぐ面 | 「全面面付き」 |
| 板材にする（厚みを付ける） | チェックした面すべてを板材に | |

- **閉じていない線も押し出せます**（ふた面だけは閉じた輪郭が要ります）。
- **複数同時に選べます**。1本が失敗しても他は作り、失敗した物は理由つきで知らせます。
- 押し出しで作った物は**元の線・面の編集に追従します**（派生）。
  元の輪郭を動かせば、先端も側面も板材も一緒に動きます。
- 「面へ投影」はご許可のとおり独立した道具として残しました。

## 3. 近似モデルの部品番号

**問題**: 電車の前面のように近似したい場所が3か所あるとき、まとめて選んでも
個別に指定できない。

**直し方**: 表に「部品」欄（数値）を足しました。

- 部品1＝オレンジの曲面、部品2＝赤い所、部品3＝緑の所、のように割り当てられます。
- 空欄（0）なら取り込んだ順に空き番号を振ります。
- **同じ番号を2つに付けたら実行前に止めます**（型紙・組立で取り違えるため）。
- できる部材モデルの名前は `<まとまりの名前>_部品N` です。
- 「形を変えずつなぐ」にした面は、隣の部品の実形状へ合うよう自動で変形した
  「〜_接続」を作ります。これで**近似された前面ユニットが確認できます**。

**「押しても近似されない」への対策**:

- 1つの面の失敗で全体を落とさず、**できる物は作り、失敗した面は理由つきで報告**します。
- 結果とエラーを**パネル内に残します**（ステータスバーの数秒表示では見逃すため）。
- **失敗があったときは表の行を消しません**（直してすぐ押し直せる）。
- 実機での再現用に、電車の前面に似たデモモデル（`_front-demo.kcd`）と、
  部品1/2/3を割り当てて実行する診断モードを用意しました。実機での結果は
  「部材モデルを3件作りました（部品1=surface_2、部品2=surface_7、部品3=surface_3）」です。

## 4. 出力モードの刷新

出力モードを、**出す物の種類**で3つに分けました。

1. **3Dモデル・プロジェクト** … 出す物を表で管理し、STL / STEP / .kcd へ
2. **平面の線図（1:1）** … 作業平面の上の線を、原寸の SVG / DXF へ
3. **板材の展開図（1:1）** … 板材1枚を紙へ展開し、原寸の PDF / SVG / DXF へ

### 出力表

- 3D画面か一覧で選んで「選択を追加」。面・板材・実体・線を入れられます。
- 「表示中の板材・実体を全部入れる」で一括投入もできます。
- **表の中身がそのまま出ます**。件数・三角形の数・閉じているかを常に表示します。
- **.kcd も表の物だけになります**。表に無い物は依存順に消し、
  表の物が参照していて消せない物（元の線・作業平面など）だけ残して件数を知らせます。
  ※ 以前は .kcd がプロジェクト丸ごと出ていました（不具合として直しました）。
- STL は、板材・実体だけなら OCCT の本物の立体から高精度で書き出します。
  面や線が混ざるときだけ、指定の板厚を与えたメッシュから書き出します。

### 3Dの確認と、自動でふさぐ機能

- 「**出力前に3Dで確かめる（別ウィンドウ）**」で別窓が開き、ドラッグで回して見られます。
- 閉じていない縁は**自動でふさぎます**。ふさいだ所は**橙色**で表示します。
- 平らでない縁や自分と交わる縁は、無理に張ると裏返った面ができて3Dプリントで
  壊れるので**ふさがず、件数を知らせます**。
- 表を変えると**その場でプレビューに反映**します。

## 5. 網羅的な試験

- **自動テスト 18本**（毎回のビルドで全部走らせています）。
  うち13本は幾何・入出力の中核で、クラウド側でも走ります。
- **画面の自動試験**（`qt_cad_smoke`）に、今回の機能を全部足しました。
  作業中グループ / 押し出し（面3枚・板3枚・追従・取り消し）/ 稜線だけの押し出し /
  出力表→プレビュー→回転→表だけの.kcd / 表示中の全取り込み / 見た目の配色と寸法 /
  エラー文の日本語化 / **カーソル操作**。
- **カーソル操作の試験**（ご指示「実際の画面やカーソル操作の具合を必ず試験」）:
  数値の上下ボタンとチェックの枠を、**画面上の当たり判定そのままの位置で実際に押して**、
  値が上がるか・切り替わるかを確かめています。Windows 95 風の寸法でも
  上下ボタンと▼ボタンが枠の内側に収まり、文字と重ならないことを確かめます。
- **実機のスクリーンショット15状態**を毎ビルドで撮り、目で見て確認しました。

### 実機の絵を見て見つけて直した問題

| 見つけた事 | 直した事 |
| --- | --- |
| 「Projected wire cannot be used as loft section.」と**英語のエラーが出ていた** | 中核・UI層の英語例外文584件の対訳表を作り、画面へ出す約120か所を通した |
| 押し出しを選んでも、左下の案内が「ワイヤーから面を作る」のまま | 道具を変えたら必ず案内も変える。出力3道具・近似4道具の手順文も用意 |
| 撮影で切り替えた見た目が設定に保存され、**次から全部 Windows 95 風で起動していた** | 撮影・試験では設定に保存しない。既定へ戻す手段も用意し、実機で戻した |
| Windows 95 風の▼▲が枠いっぱいに引き伸ばされて巨大 | 一次資料どおり固定寸法（幅7px・高さ4px）で中央に描く |
| スピンの上下ボタンが低すぎて矢印がはみ出す | 幅16px・枠の内側いっぱいの高さに置き直す |
| 出力の .kcd がプロジェクト丸ごと出ていた | 表の物だけにする（単体試験つき） |
| 同じ物を出す道が2つあった（出力表と旧「3DモデルのSTL/STEP出力」） | 旧UIを廃止し、役目（表示中の全取り込み・除外グループ以外の.kcd）を表側へ移した |
| 「板材にする」を外しても板厚・材料を触れてしまう | チェックに合わせて隠す |
| 近似で失敗しても表が空になり、選び直しになる | 失敗があるときは行を残す |

## 6. 画面の分かりやすさ（ChatGPT／Codex との相談）

Codex（gpt-5）に「初めて使う人がつまずく所」を厳しく挙げさせ、重大な順に反映しました。
指摘の全文は PC の `_codex-review2.md` にあります。反映した主なもの:

1. **案内が道具に追従していない**（最重要）→ 直しました（5章の表）。
2. **「3Dモデルを出力する…」を押しても保存されない**（プレビューが開くだけ）
   → 「**出力前に3Dで確かめる（別ウィンドウ）**」に改名。橙色の意味もボタンの脇に常時表示。
3. **出力表が全出力に効くと誤解される** → 見出しを「STL / STEP / .kcd に含めるもの」にし、
   「この表は1:1図面と展開には使いません」を常時表示。道具名も出す物で分けました。
4. **近似モデルの専門語** → 役割の選択肢を
   「**板に分ける／形を変えずつなぐ／使わない**」に。表の上に常設の4手順。
   「ユニット名」→「まとまりの名前」、「ユニットを近似」→「まとめて部材にする」。
5. **主手順と従来方式が競合** → 「1面だけ帯状に分ける（詳しい設定）」として畳み、既定で閉じました。
6. **押し出しと厚み化のどちらを使えばよいか分からない** → 画面の先頭で目的を二択にし、
   選んだ側の入力欄だけ出します。
7. **展開の初期画面が重すぎる** → まず「板材・作り方・形の忠実さ・用紙・1:1 PDF」だけ。
   切れ目・折り目・組立途中は「詳しい設定を出す」の中へ。
8. **用語** → 保存（プロジェクト）と書き出す（外部形式）を分け、
   「立体再現度」→「形の忠実さ」など、結果が読める言葉に変えました。

**まだ入れていない指摘**（次回の候補として残します）:
「面を作成」の自動／おまかせ／ゴードンの重複整理、各道具の共通の「入力」枠と
実行前の検証行、選択情報パネルへの常設の入口、右クリック専用操作の代替ボタン。

## 7. Windows 95 風の見た目

**設定メニュー →「Windows 95 風の見た目」**で通常版と切り替えられます。設定は保存されます。

一次資料に当たって実装しました。

- *The Windows Interface Guidelines for Software Design*（Microsoft Press, 1995）
  第13章 Visual Design の Basic / Button / Field Border Styles
- Win32 `DrawEdge` の BDR_・EDGE_ の定義
- コントロールパネルの配色の既定値

守っている点:

- **配色**: 面 #C0C0C0 / 明 #FFFFFF / 淡 #DFDFDF / 影 #808080 / 濃影 #000000 /
  入力欄の地 #FFFFFF / 選択 #000080（文字は白）/ ヒント #FFFFE1
- **縁は必ず二重**。浮き上がりは外（左上＝白／右下＝黒）＋内（左上 #DFDFDF ／右下 #808080）。
  押し込みはその逆。彫り込みは1pxずつ。
- **チェックボックスは13×13**の沈んだ白枠に黒いチェック、**ラジオは12×12**。
  ご指示のとおり、チェックの有無が見て分かります。
- **数値の上下△▽**は幅16px（システムの縦スクロールバー幅）で枠の内側いっぱい。
  **矢印は伸び縮みしない固定寸法**（幅7px・高さ4px、7/5/3/1 の三角）です。
  コンボの▼も同じ寸法・同じ浮き上がりの縁で描きます。
- スクロールバーは16px、溝は白と面色の市松。ボタンは押すと中身が1pxずれます。
  フォーカスは点線の枠。文字は **MS UI Gothic 9pt（アンチエイリアス無し）**。
- 一覧・表・ツリーの地は白（COLOR_WINDOW）のままです。

見た目を変えても操作できることを、当たり判定の位置での実クリックで試験しています。

---

## 8. 板材と面の違い（ご質問への回答・再掲）

- **面**：厚みのない理想の形そのもの。曲面の当たりを取る、境界を決める、
  他の面をつなぐ、といった**設計の途中の物**です。
- **板材**：面に**厚み・厚みの向き・材質**を与えた、**実際に切り出す物**です。
  開口（窓・穴）、切れ目、分割線、積層、展開図、STL / STEP はすべて板材が対象です。

「押し出し・厚み化」で面から板材を作り、出力モードで板材から製作データを出す、
という流れになります。

---

## 9. 使ううえで気を付けていただきたい点

1. **作業中グループ**はツールバーで確認してください。ここを切り替え忘れると、
   新しく引いた線が意図しないグループに入ります（後から移せます）。
2. **押し出しの「ふた面」は閉じた輪郭にだけ**作れます。開いた線でチェックすると
   その旨を知らせて、他の物だけ作ります。
3. **STEP は板材・実体だけ**書き出せます。面や線は先に板材にしてください
   （押し出しの「板材にする」）。STL は面・線でも板厚を与えて書き出せます。
4. **出力前の3D確認で橙色が多いとき**は、元の面がつながっていないサインです。
   そのまま保存もできますが、3Dプリントの前に元の面を見直すことをお勧めします。
5. **近似で「使わない」にした行**は何もしません。表に入れたのに結果に出ないときは、
   役割の欄を見てください（結果の文にも「使わなかった行」として出ます）。

---

## 10. 次にやると良いこと

1. 「面を作成」の自動／おまかせ／ゴードンの重複整理（Codex指摘 中）
2. 各道具の共通の「入力」枠（選択中／登録済み）と、実行ボタン直上の検証行（同 中）
3. 姿勢計算の一本化（プレビュー・派生再生成・実体化を同じ関数へ）
4. 近似モデルの輪切り断面線を派生ワイヤで出す（胴枠・治具設計の土台）
5. 部材面の回転対応（いまは平行移動のみ）

---

*作業メモ（環境・手順・ハマりどころ）はプロジェクトの
`claude/作業メモ-検証環境と改修状況.md` に更新済みです。*
