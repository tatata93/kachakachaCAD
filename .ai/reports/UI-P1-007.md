# UI-P1-007 worker report

I made the reviewer's must-fix changes, but **I could not build or run any tests**. The session blocks `cmake`, and nobody could approve it, so none of the checks below have run.

# Changed files
Files changed in this revision run:
- `src/next/kachakacha/app/Selection.cpp`: edge picking now uses `tolerance.edgePickPx` (was `displayPickPx`). Point picking still uses `displayPickPx`.
- `src/next/kachakacha/app/Selection.h`: the comments for `PickCurve` and `PickPoint` now name the separate radii.
- `tests_v2/selection_tests.cpp`:
  - The `Tolerance()` fixture sets `edgePickPx = 6.0`.
  - "拾う範囲は許容差で決まる" now changes `edgePickPx`, and two old 8px comments now say 6px.
  - Added `PickPoint` and two new tests.
- `tests_v2/grab_to_move_tests.cpp`: fixed a comment (the line radius is `edgePickPx`, 6px).
- `docs/manual/作り方.md` §3-3: "`Shift` stops snapping" → "`S`", and "high 6px / low 8px" → a single 12px screen radius that doesn't change with zoom.
- `docs/manual/README.md:178`: "`Shift`" → "`S`" so the two manual pages agree. The app already uses S (`V2ViewportInput.cpp:216`).

The rest of the diff is from the earlier Stage 1 attempt and I left it unchanged: `GeometryTolerance.h`, `SnapEngine.h/.cpp`, `DocumentFile.cpp`, `DocumentFileRead.cpp`, `V2SelfTestScreen.cpp`, `snap_tests.cpp`, `foundation_tests.cpp`, `document_file_tests.cpp`, `geometry-contract.md`, `kcd2-format.md`, `v1-drawing-parity.md`, and the two sample `.kcd2` files.

# Implementation
Plan: apply only the three must-fix items from `self-revision-stage-1-1.md`.

1. **Edges use their own 6px radius.** `CollectCurvePicks` now uses `edgePickPx`. Everything that picks edges (`PickCurve`, `PickEntity`, `CollectPickCandidates`, `PointerGrabsSelection`) goes through it, so all of them now pick lines at 6px and points at 8px. New tests:
   - "線は6px_点は8pxで拾う" uses the default values. A line is picked at 5px but not at 7px. A drawing point is picked at 7px but not at 9px.
   - "点と線の拾い半径は互いに影響しない": changing `displayPickPx` (to 50 or 1) doesn't change edge picking, and changing `edgePickPx` (to 1 or 50) doesn't change point picking.
2. **Manual.** It now describes what the app actually does: 12px, and S instead of Shift. I left out the reviewer's suggested "plus the hold margin" wording on purpose. The hold is not connected in the app yet (see Remaining issues), so the manual would describe behaviour users can't get.
3. **Unfinished requirements** are listed under Remaining issues.

I left `V2ViewportInput.cpp:843` on `displayPickPx`. It only decides whether the Tab candidate list stays on "the same spot"; it is not a pick radius.

# Build result
**NOT RUN.** `cmake --preset windows-msvc` was denied by the permission system, so neither configure nor `cmake --build --preset windows-msvc --parallel` ran. The earlier PASS results in `self-review-packet-stage-1.md` are from before this revision and don't cover these edits.

# Test result
**NOT RUN**: `ctest --preset windows-msvc` and `build-msvc2022-x64\Release\kachakacha_cad_next.exe --self-test`. The Stage 1 acceptance test ("existing build and tests succeed") is therefore **not verified**.

Checks I did by reading the code instead:
- Every existing edge pick in `selection_tests.cpp` and `grab_to_move_tests.cpp` is 0, 2, 5 or 200px from the line. None fall between 6 and 8px, so none should change result.
- `manual_tests.cpp` checks chapter counts, images, export tables and diagnostic codes, not the snap wording.
- The app self-tests (`V2SelfTestScreen.cpp`, `V2SelfTestInput.cpp`) were not checked for clicks 6–8px from a line. They could be affected.
- A search of the changed code files found no TODO, FIXME, stub or placeholder markers.

# Diff summary
`git diff --stat`: 20 files, +580 / −84. This revision adds 6 files (Selection.cpp/.h, selection_tests, grab_to_move_tests, both manual pages), about +57 / −9 lines. No V1 files, no new files, no Qt or OCCT in `src/next`. `git diff --check` was blocked with the rest of that combined command and not run.

# Remaining issues
1. **Build, CTest and app self-test not run** (permission denied). They must run before this Stage counts as done.
2. **Hysteresis is not connected in the app (Stage 2).** Until it is, small pointer shakes still switch candidates in the app, and S doesn't clear a held snap there.
   - `DrawingSession::Hover` (`DrawingSession.cpp:81-83`) still calls `ChooseSnap` without any held state.
   - Nothing calls `SnapHysteresis::Reset` on tool switch, Esc or step-back.
   - `V2SelfTestInput.cpp` is unchanged.
3. **Stronger snapping for the active tool is missing** (§6.1 "現在ツールに適合する候補で強さを変える", §4.3 item 1). The diff uses one 12px radius and only ranks by kind.
4. **Values chosen by the worker, not by §6.1; the owner needs to confirm them:**
   - Extension and ProjectedOnPlane rank below perpendicular/tangent and above grid.
   - FreeOnPlane has its own rank below grid.
   - `holdMarginPx = 4`.
5. **Minor, not fixed:** `DocumentFileRead.cpp:689-698` accepts 0 or negative `edgePickPx`/`snapPickPx`, which turns off edge picking or snapping. `displayPickPx` already behaves this way, and `kcd2-format.md` §19.3 doesn't forbid it.
6. **Stale comment outside scope:** `V2ViewportInput.cpp:7` still says Ctrl pauses snapping. The code uses S.


# Claude self-review

PASS

# UI-P1-007 Stage 1/2 self-review (STAGED, EXTRA_HIGH)

I checked the full diff (20 files, +580/−84) against `CURRENT_TASK.md`, the UI-P1-007 acceptance tests in `.ai/TASKS.json`, spec §4.3 and §6.1, and the three must-fix items in `self-revision-stage-1-1.md`. I made no edits.

## Build and tests
- The worker report says **NOT RUN**, but that is out of date. The orchestrator ran all four gates after the worker's last edit. The last edited file was saved at 11:09:20 and the gates ran 11:10–11:16:
  - configure: PASS
  - build: PASS. It shows only two warnings, both in files this diff doesn't touch.
  - CTest: 129 of 129 passed, including `manual_tests`, the selection, snap and document file tests, and the `.kcd2` sample byte comparisons.
  - app self-test: 168 of 168 passed.
- So the Stage 1 acceptance test ("existing build and tests succeed") is verified for the current tree.

## The three must-fix items are done
1. **Separate edge radius.** `Selection.cpp:187` now uses `edgePickPx`. Point picking at `:217` still uses `displayPickPx`, and the comments in `Selection.h` match.
   - New tests: "線は6px_点は8pxで拾う" checks lines at 5/7px and points at 7/9px with the default values. "点と線の拾い半径は互いに影響しない" checks that changing one radius doesn't move the other, in both directions.
   - I checked the existing pick distances in the selection and grab-to-move tests (0, 2, 5, 20 and 200px). None fall between 6 and 8px, so none change result.
2. **Manual.** Both manual pages now say `S` and a single 12px radius. That is what the app currently does, because the hold is not connected yet. §6.2 matches.
3. **Unfinished requirements.** They are all listed: the app wiring (`DrawingSession.cpp:81-83`, `Reset` calls, `V2SelfTestInput.cpp`), tool-dependent snap strength, and the worker-chosen values that need owner confirmation.

## Stage 1 against the task and the specs
- **Separate 6 / 8 / 12px values.** They are separate fields in `GeometryTolerance`, documented in `geometry-contract.md`, saved and loaded in `.kcd2` (§19.3 matches), and the default-value test is updated.
- **Zoom.** The snap radius has a test at 10 px/mm and at 1 px/mm. Picking measures distance in screen px through `ScreenMapping`, so its radius doesn't change with zoom either, but no zoom test covers picking yet.
- **Hysteresis.** `SnapHysteresis` and `SameSnapTarget` provide the state for Stage 1.
  - `ChooseSnap` can only hold a candidate of the same or lower rank than the best one. A higher-ranked candidate takes over at once; a same-rank candidate takes over only when it is nearer by more than `holdMarginPx`.
  - Only the held target gets the wider radius. Tests cover jitter, the radius edge, higher-rank takeover, and which targets count as the same.
- **S key.** `Resolve` clears the held snap while S is held. It is tested, including after S is released.
- **§6.1 ranks.** `SnapPriorityRank` matches the six tiers. The old test that ranked intersections above endpoints was correctly reversed.
  - The old rule "screen distance beats priority when distances differ a lot" is gone. It is not in the integrated spec, and no document under `docs/` still contains it.
- **Risk from reordering `SnapKind`.** Nothing converts `SnapKind` to an integer for saving or display. The only integer use is the tie-break in `SnapEngine.cpp:308`. The Tab cycle in `V2ViewportInput` lists pick candidates, not snap candidates, so it isn't affected.
- **Removed API.** Nothing in the repo still refers to `highPriorityRadiusPx`, `lowPriorityRadiusPx` or `IsHighPrioritySnap`. `DrawingSession` compiles and keeps its old stateless behaviour, apart from the new §6.1 order.

## Unrelated changes, hygiene, error handling
- The file changes outside the task's candidate list are justified:
  - The samples had to be regenerated for the byte comparison.
  - `V2SelfTestScreen.cpp` follows the larger snap radius and still checks that the point can be selected.
  - The docs and manual had to change because of the code change.
  - `Selection` and `grab_to_move` were required by the earlier review.
- No V1 files, no untracked files, no TODOs or stubs, and no Qt or OCCT in `src/next`.
- `DocumentFileRead` accepts a zero or negative `edgePickPx`/`snapPickPx`. `displayPickPx` already behaves this way and §19.3 doesn't forbid it, so it is recorded as minor and doesn't block.

## Carry into Stage 2 (not blocking Stage 1)
1. **Connect the hysteresis in the app.** `DrawingSession::Hover` should use `SnapHysteresis`. Call `Reset` on tool switch, Esc and step-back, and add app self-tests in `V2SelfTestInput.cpp` for pointer jitter and holding S.
2. **Snap more strongly for the active tool** (§6.1, §4.3 item 1), or get an explicit decision from the owner to defer it.
3. **Add a zoom test for picking.** Check that edges pick at 6px and points at 8px at two zoom levels, covering the acceptance test "ズーム倍率にかかわらず画面上の選択半径を保つ" for picking, not just snapping.
4. **Get owner confirmation** for the Extension/ProjectedOnPlane rank, the FreeOnPlane rank and `holdMarginPx = 4`.
5. **Fix the stale comment** at `V2ViewportInput.cpp:7`, which says Ctrl stops snapping.

## Local gates

- PASS `cmake --preset windows-msvc`
- PASS `cmake --build --preset windows-msvc --parallel`
- PASS `ctest --preset windows-msvc`
- PASS `C:\Users\tak01\github\kachakachaCAD-worktrees\ui-p1-007\build-msvc2022-x64\Release\kachakacha_cad_next.exe --self-test`

## Diff stat

```text
docs/manual/README.md                              |   2 +-
 .../\344\275\234\343\202\212\346\226\271.md"       |   4 +-
 docs/v2/geometry-contract.md                       |   7 +-
 docs/v2/kcd2-format.md                             |   5 +-
 docs/v2/v1-drawing-parity.md                       |   2 +
 samples/streamlined-railway-nose-1-87.kcd2         | Bin 73629 -> 73672 bytes
 samples/v2-sample.kcd2                             | Bin 22950 -> 22993 bytes
 src/apps/cad_next/V2SelfTestScreen.cpp             |  15 +-
 src/next/kachakacha/app/Selection.cpp              |   2 +-
 src/next/kachakacha/app/Selection.h                |   4 +-
 src/next/kachakacha/geometry/GeometryTolerance.h   |   6 +-
 src/next/kachakacha/io/DocumentFile.cpp            |   2 +
 src/next/kachakacha/io/DocumentFileRead.cpp        |  10 +
 src/next/kachakacha/modeling/SnapEngine.cpp        | 154 ++++++++---
 src/next/kachakacha/modeling/SnapEngine.h          |  76 ++++--
 tests_v2/document_file_tests.cpp                   |  28 ++
 tests_v2/foundation_tests.cpp                      |   2 +
 tests_v2/grab_to_move_tests.cpp                    |   2 +-
 tests_v2/selection_tests.cpp                       |  57 +++-
 tests_v2/snap_tests.cpp                            | 286 ++++++++++++++++++++-
 20 files changed, 580 insertions(+), 84 deletions(-)
```
