# TASK_LEDGER — UI 再構築(2026-09-18 開始)

正本は `UI_FEATURE_MATRIX.md`。ここは Stage ごとの実装単位と検証結果。
「検証」は必ずこのセッションの tool 出力に紐づける(雲 = core ctest / 当て木 / 自己試験コード、PC = run.txt / png)。

## Stage 一覧

| Stage | 内容 | 状態 | 検証 |
|---|---|---|---|
| S0 棚卸し | 3 HTML + repo の突き合わせ、3文書作成 | DONE(2026-09-18) | 雲: subagent 3本の inventory を MATRIX へ反映 |
| S1 Common | core `app/Ribbon`(mode→category→tool)、`V2Ribbon`、Explorer 階層 + 個別表示 + scroll-to + 「グループ」、右ペイン1道具1枚、HUD/状態行、測定の重ね道具 | CLOUD_DONE(2026-09-18) / PC 未検証 | 雲: ctest 148/148、typecheck 97 files、自己試験 HP-RB-01〜03 / HP-XP-01〜04(旧 HP-EX-01〜04。Extrude の HP-EX-01〜03 と ID 衝突していたため改名) / HP-ST-01〜03 を追加(PC で実行) |
| S2 Drawing-A | 基本作図/曲線の作り方カード(円/円弧/ベジェ/スプライン)、Tool 切替で panel 同期 | CLOUD_DONE(2026-09-18) / PC 未検証 | 雲: drawing_method_cards_tests、HP-DM-01〜03(PC で実行) |
| S3 Drawing-B | 編集/変形/作業面(12方式 + 下見)/注記 | PARTIAL(作業面の下見 D-24 = 雲側完了) / PC 未検証 | 雲: HP-WP-01。編集/変形/注記の欄は未 |
| S4 Drawing-C | 面作成(6主要 + その他)の新 panel、穴の明示 | DONE(2026-09-22〜23、自由曲面の P1〜P7)/ PC_TESTED(66d3cac) | PC: ctest 165/166(落ちは試験の不具合で直した)、自己試験 309/309。下の「自由曲面と入力の数」 |
| S5 Part-A | 押し出し Inventor 型 panel(From/To/範囲/方向/演算/出力)、厚み、回転体案内 | CLOUD_DONE(押し出し棚 + 厚みの道具化) / PC 未検証 | 雲: HP-PA-01/02、HP-TH-01/02。回転体は帯で理由つき Blocked |
| S6 Part-B | 形状編集/面編集/ブール/配置(実装済み + disabled+理由) | NOT_STARTED | |
| S7 Fab-A | 近似(作り方4、候補カードに方式/平均誤差の扱い、表示) | PARTIAL(作り方カード = 雲側完了) / PC 未検証 | 雲: approx_input_tests、HP-AP-03/04。候補カードの方式/平均誤差の表示(F-03)と表示切替(F-04)は未 |
| S8 Fab-B | 部材編集(3D で部材を押す、分割/結合/切れ目/半径) | NOT_STARTED | |
| S9 Fab-C | 曲げ(slider/preset)/展開/生成、Explorer の Approximation 節 | PARTIAL(曲げの基準値/スライダ、生成カード = 雲側完了) / PC 未検証 | 雲: HP-AP-04、HP-GN-01/02。展開(F-11/F-12)は未 |
| S10 Integration | 回帰試験、寸法/拡大率撮影、旧 UI 片づけ、verifier | PARTIAL(I-01 回帰試験、I-03 の一部 = 雲側完了) | 雲: REGRESSIONS.md、RG-02/12/14。撮影(I-02)と verifier(I-04)は未 |

## 単位の記録

(Stage を閉じるたびに追記: commit / 何を実装 / 雲の結果 / PC の結果 / 残り)

### S0 棚卸し — 2026-09-18
- 雲: `docs/v2/ui-redesign/{UI_FEATURE_MATRIX,TASK_LEDGER,UI_LESSONS}.md` 作成。
- backend に無いもの(BLOCKED_BACKEND)は MATRIX の各行に明記。UI では disabled + 理由で出す。

### S1 Common — 2026-09-18(雲側完了、PC 未検証)
- commit `ce52688` 帯: core `app/Ribbon`(mode→category→tool、Blocked 理由つき)+ `V2Ribbon`(2段、setDefaultAction)。面作成は作図モードへ。HP-RB-01〜03。
- commit `d3fafae` Explorer: core `app/ExplorerModel`(節と所属)、`V2ExplorerBuild.cpp`(文書 → 8節 → 原点/グループ/もの、◉ で1つずつ出し隠し = SetVisibilityCommand)、`V2ExplorerMenu.cpp`(右クリック献立は台帳の QAction のみ、グループへ移動は落とすのと同じ道)、「まとまり」→「グループ」。HP-XP-01〜04(旧 HP-EX-01〜04。Extrude の自己試験と ID が衝突していたため改名、2026-09-19)。
- commit `2ce29b0` 状態行/HUD/測定: core `app/StatusLine`(左右の文言と HUD)、`EscapeAction` に ResumeToolAfterMeasure、`V2StatusLine.cpp`、`V2ViewportHud.cpp`、右の欄の見出し下に案内、ShelvesFor は押し出し/面/ブールの最中 1枚だけ。HP-ST-01〜03。
- 雲: ctest 148/148(status_line_tests, explorer_model_tests, ribbon_tests を追加)、qt stub typecheck 97 files OK。
- PC: 未実施(`_GO.cmd` → Release build → 自己試験 → 撮影 が要る)。**PC_VERIFIED ではない。**
- 残り(S1 のうち後ろへ回したもの): 道具パネルの共通枠(C-10)は「見出し + 案内」まで。作り方/入力/オプション/確定・キャンセルの段組みは S2 以降で各 Dock を作り替えるときに。測定の重ね道具は道具の種類を戻す(打ちかけの点は残らない = ToolController の仕様)。

### S2 Drawing-A — 2026-09-18(雲側完了、PC 未検証)
- core `app/DrawingMethodCards`(道具 → カード。核に無いものは理由つき、円弧の始点接線は「その他」)。
- `V2DrawingDock`: 円弧の作り方コンボを廃し、作り方カード(押せる/押せない + 理由)+ カードの一文 + 入力 + オプションの段組みに。カードの押下が ArcMode を決める。
- HP-DM-01〜03(帯 → カード → 3D で円弧、スプライン/ベジェのカード)。
- 正本の HTML と指示書を `docs/v2/ui-redesign/mocks/` に置いた(次のセッションが読めるように)。

### S5 Part-A(押し出しの棚)— 2026-09-18(雲側完了、PC 未検証)
- `V2ExtrudeDock`: 範囲 5 通り(距離/左右対称/両方向に別々の距離/選んだ面まで/全部貫く)と方向 7 通りを棚に全部。逆側の距離・相手の面(作業平面)・向き x y z は選んだときだけ生える。開始面(From)とテーパーは押せない形 + 理由。
- `PrepareExtrudeChoice` / `RefreshExtrudeFromDock` が棚の範囲・逆側の距離・相手・向きの数を読む(詳細の窓は残るが棚だけで全部決められる)。
- HP-PA-01/02。回転体は帯で Blocked(作図の回転面 + 厚み へ案内)。

### S7 Fab-A(作り方カード)/ S9 の曲げ状態の基準値 — 2026-09-19(雲側完了、PC 未検証)
- commit `392646e`: core `ApproxPolicy`(標準/少部品優先/精度優先/手動条件)+ `CandidateForPolicy`。`V2FabricationDock` に作り方カード 4 枚、曲げ状態のスライダと基準値 0/25/50/75/100(組立率を打って当てるのと同じ道)。近似モデルが無いときの「当てる」は理由を状態行へ。
- HP-AP-03(作り方が既定の候補を決め、押した候補が勝つ)、HP-AP-04(基準値は組立率を打って当てる)。

### 2026-09-19 のまとめ(雲側完了、PC 未検証)
- 0fda226 部品「厚み」を 道具 → 3D で面 → 作り方 4 枚 → 下見(kernel 空回し)→ Enter に(P-10、HP-TH-01/02)。既存試験は 1 度目 = 構え、2 度目 = 確定 に。
- 773af56 生成の作り方カード 現在状態 / Flat 0% / Target 100%、fabrication.freeze_target、固定は 1 回の取り消し(F-13/F-14、HP-GN-01/02)。
- d9f6945 作業面の下見(D-24、HP-WP-01)。4b3ec7c 候補の行に方式と平均「—」(F-03)。
- 6cdbe98 既知の退行 15 件の固定表 REGRESSIONS.md、RG-02/12/14(I-01)。40e8550 部品モードの 2 枚目(形状ガイドの役割の表)を廃止(I-03 の一部)。
- **PC ではまだ何も実行していない。** 次: `_GO.cmd` → `_claudeout\ctest.txt` の `cad_next self-test:` 行を読む。

### 2026-09-19 PC 検証(オーナーが 19:52 と 20:20 頃に `_GO.cmd`)
- 1 回目(6536509): build OK、自己試験 274/288。落ちた 14 件の原因 5 つを ca4ee28 で手当て。
- 2 回目(ca4ee28): build OK、自己試験 **286/288**。残り HP-SF-06 / HP-TH-02(押した当人を欄へ伝える)を d3b6657 で手当て。
- PC 画面(`_claudeout\ui\*.png`)で見えた不具合 3 つ(帯の右寄り、一覧の名前が消える、近似の作り方が 2 枚しか見えない)を c7c1e53 で手当て。帯の道具に合わせた棚の段の切り替えと HUD の幅を 1a42d92 で。
- オーナー方針(09-19): **スイープなど新機能はまだ作らない。実用に耐える UI にする。** 以後は既存機能の使い勝手と PC 検証を優先。
- 次の `_GO.cmd`(1a42d92 以降)で確かめること: 自己試験 288/288、`ui/` と `resp/`(3 サイズ × 3 倍率)の撮影。使い方は `HOWTO-実用手順.md`。

### 2026-09-19 PC 検証 3 回目(4f70d62)と Stage Integration の手当て(5e4c27f)
- 3 回目の結果(`_claudeout\ctest.txt` / `run.txt` を雲で読んだ): build OK。ctest の `v2_cad_next_smoke` が **900 秒でタイムアウト**: 自己試験が `RUN HP-AR-01` の直後で止まっていた(V2MainWindow が据え付けていた配列の窓 V2ArrayDialog の `exec()` が閉じられない)。`v2_package_zip` の自己試験(配布 exe)は HP-XP-02 の途中で落ちた(PASS/FAIL 無しで終了)。実際に FAIL と出たのは HP-SF-06(「やめると 3D の印も消える」)だけ。オーナーの貼った要約に出ていた「数値入力が画面の外へ出ない」は PASS しており、HP-AR-02 / 組立率と半径… / HP-RS-01 は HP-AR-01 で止まったため未実行。
- 手当て(5e4c27f): (a) 配列は棚だけ(V2ArrayDialog 削除、V2ArrayChoice.h へ分離、HP-AR-01/02 は `SetArrayChooser(nullptr)` で棚の道を通す) (b) 組立状態 `fabrication.set_assembly` は窓(V2NumberDialog 削除)を出さず製作の棚 2 段目の組立率の欄へ案内(新 HP-FB-05) (c) EndSurfacePreview で 3D の選択を空にする(HP-SF-06) (d) ToggleEntityVisibilityFromItem が木の作り直し後に消えた行の `item->text(0)` を読んでいた(use-after-free)→ 名前と id を先に写す (e) V2FabricationDock / V2EditDock を 380px に収める(QFormLayout WrapLongRows + AllNonFixedFieldsGrow、基準値ボタンを別行 40px、文言短縮、xyz 欄 NoButtons、横スクロール禁止) (f) 手順の一覧(processDock)は既定で畳み、表示メニュー「手順の一覧」で出す。選んでいる数は選択から数え直す(RefreshProcessContextFromSelection) (g) 状態行と HUD は棚で進める操作(押し出し/面を作る/足す引く/厚み/近似/面取り/配列)の名前を道具名に出す(RunningOperationNameJa、新 HP-ST-04) (h) main.cpp の撮影を devicePixelRatio ぶん大きな絵に(resp の倍率違いの絵が全部同じだった)。
- 雲の門: typecheck OK (111 files)、ctest 150/150。自己試験は 291 件になる見込み(288 + HP-FB-05 + HP-ST-04 + 既存)— PC で数える。
- 4 回目の `_GO.cmd` は Claude が Git CMD(計算機操作)から起動した(2026-09-19 18:59 頃)。結果はこの下に追記する。
- 古い重複 UI の棚卸し(subagent 調査): 残る要検討は `guide.create`(形状ガイド(旧))が「形」メニューに残っていること(自己試験 6 か所が場面づくりに使うので今回は残置)、`V2ChoiceDialog`(guide.set_method / guide.append_row の窓、面作成の内部実装として現役)。QDockWidget 派生の各棚は V2OperationPanelHost のページとして現役で削除対象なし。
- 注意: V2MainWindow.h は 1499 行(上限 1500)。次に足すときは accessor を別ヘッダへ分けること。

### 2026-09-19 PC 検証 4 回目(5e4c27f、Claude が Git CMD から `_GO.cmd`)
- build OK。ctest の smoke は 111 秒で完走(ハング解消)。自己試験 **292/293**(ctest の smoke と配布 exe の両方で同じ)。落ちたのは HP-RS-01 の「ui-fab-generate が作れる」だけ。単独の撮影(`--manual-state ui-fab-generate`)は成功しており、前の場面(厚み: 面を 1 枚作った)の後に続けて作ると作れない。原因: EndSurfacePreview が欄の線(surfaceInput_ の断面など)を次の面へ持ち越していた(別の文書では消えた id を指す)。→ 手当て: 面の道具をやめるとき欄を空にする(作り方は残す)。ApplyUiShotState は毎回 作図モード・選択道具 から始める。HP-RS-01 は落ちたときの案内文を出す。
- 絵(`resp/`): 製作の棚・編集の棚は 1280x720 で横スクロール無しに収まった。手順の一覧は畳まれた。HUD は「部品 › 厚み」。1.5 倍の絵は 1920x1080 になり(この PC は 150% 表示)、作図の棚の作り方カード 4 枚目が切れて横スクロール、状態行の右端「Enter 確定」が切れていた → 作り方カードは 3 枚ごとに折り返す升目、状態行の案内は Ignored で縮む。
- 注意: QT_SCALE_FACTOR=1 でも絵は 1920x1080(devicePixelRatio 1.5)。この PC の表示倍率が効いており、倍率の輪(1/1.25/1.5)は実質 1.5 固定と見るべき。倍率を本当に変えて撮るなら `QT_SCALE_FACTOR` より `QT_ENABLE_HIGHDPI_SCALING=0` などの組み合わせが要る(未対応、記録のみ)。

### 2026-09-19 PC 検証 5 回目(8005c20、Claude が起動)— **ctest 157/157**
- ctest 全部通過(smoke の自己試験 293/293、配布 exe の自己試験も通過)。1.5 倍の絵で作り方カードの折り返しと状態行の右端を確認。
- ただし `_FIX_AND_BUILD.cmd` が最後に直接走らせる `--self-test`(実画面)だけ 291/293: 「数値入力が画面の外へ出ない」(実画面では本物のマウス位置が擬似ホバーと混ざる)と「組立率と半径…」(半径 0.01 mm 丸めで 2 倍が最後の桁 1 つずれる: 50.545→50.55、2 倍 101.09)。→ 直の自己試験も ctest と同じ offscreen で走らせる(reference.cmd、`_FIX_AND_BUILD.cmd` 再生成)。丸めの試験は最後の桁 1 つ分を許す(意味は同じ: 2 倍になる)。
- これで PC 側の門は ctest 157/157 = **PC_VERIFIED(8005c20)**。絵: `_claudeout/resp/*`(3 サイズ × 1.5 倍実効)。

### 2026-09-19 古い重複 UI の片づけ(統合の続き)
- `guide.create`(形状ガイド(旧))を台帳・献立・モードの一覧・説明書から外した。人の入口は「面を作る」1 つ。中身(選んだ線を全部断面にして一気に作る道)は `V2MainWindow::CreateGuideSurfaceFromSelection()` として残し、場面づくりと「断面が2つ以上要る」の断りを見る自己試験 6 か所だけが直接呼ぶ。
- 出力モードの右を 1 枚に(`{Export, Pattern}` → `{Export}`)。型紙の下見は `fabrication.create_pattern` が作ったときに自分で前へ出す(棚の到達性の関所に「自分の命令が出す棚」として登録)。HP: 「型紙の下見は何枚目かと紙の大きさを言う」に、出力モードの右が 1 枚であることと、作ると型紙の棚が前に出ることを足した。
- 残る 2 枚組みは製作モードの `{Fabrication, Parameter}` だけ。数の棚(板厚・許すずれ・型紙の余白・縮尺)は製作の数そのものなので、いまは残す(UI_FEATURE_MATRIX C-09 に理由)。
- `V2MainWindow.h` は 1492 行(上限 1500)。次に足すときは分割する。

### 2026-09-20 PC 検証 6 回目(65b2328)= **PC_VERIFIED**
- ctest **157/157**、配布 exe と smoke の自己試験 **293/293**、直の自己試験(offscreen)も 293/293。`_claudeout/resp` に 45 枚(3 サイズ × 5 場面 × 3 倍率)。
- 絵の実寸: 1280x720 の 1 倍 = 1920x1080(この PC の表示倍率 150% が効く)、1.25 倍 = 1600x900、2560x1440 は画面の高さで 2560x1421 に切られる。**Windows の表示倍率そのものを変えた確認はできていない**(倍率は QT_SCALE_FACTOR で代用)。ここは正直に未達として残す。

### 2026-09-20 人の道の穴埋め(検証役の指摘)
- 別文脈の検証役に統合段を点検させた。出てきた本物の穴:
  1. 円・ベジェ・スプラインは「カードが出る」ことしか見ておらず、**画面を押して実際に引く試験が無かった** → HP-DM-04/05/06 を追加(円は中心＋半径と直径指定の両方、ベジェは 4 点、スプラインは 4 点 + 確定)。
  2. 円の「直径指定」カードは押しても何も変わらなかった(核に CircleMode が無く、2 点の距離が常に半径)。→ カードに「名指しする入力欄」(`cursorFieldId`)を持たせ、直径指定を選ぶと 3D のカーソル入力列が「直径」の欄を選んだ状態で出るようにした。名指しが実在する欄かは core の試験で見る。
  3. 押し出しの棚の試験が combo の signal を塞いで直に値を入れていた(signal 配線が壊れても気づけない)。→ `V2ExtrudeDock::PickExtent/PickDirection`(signal を塞がない、人が選ぶのと同じ道)を足し、HP-PA-01 を「選んだだけで作る形の範囲が変わる」で見るようにした。映すだけの `ChooseExtent/ChooseDirection` は棚を書き直す側の道として残す。
  4. 台帳に無い id を押すと黙って何も起きなかった(`RunCommand` の入口)。→ 「台帳にありません」と言う。
- `V2Viewport.cpp` が 1500 行に届いたので、カーソル入力列を `V2ViewportCursor.cpp` へ分けた(中身は不変)。

### 2026-09-20 PC 検証 7 回目(1dd9c36)
- ctest **157/157**、自己試験 **296/296**(293 + HP-DM-04/05/06)。円・ベジェ・スプラインを画面から実際に引く道と、「直径指定」で入力列が直径の欄で出る道が、実機で通った。

### 2026-09-20 PC 検証 8 回目(36cbfb6)= **統合段(Integration)完了**
- ctest **157/157**、自己試験 **296/296**、`_claudeout/resp` に 45 枚。1280x720 の 1.5 倍で製作の棚に横スクロールが無く、状態行の右端(Enter 確定)も切れていない。2560x1440 でも一覧・3D・右の棚がどれも欠けず潰れない。
- 統合段の 4 つの持ち物の現在地:
  - 人の道の回帰 … 既知の退行 15 件は全部どれかの試験が押さえている。加えて、検証役が見つけた「人の道と言いながら実際には引いていない」3 件(円・ベジェ・スプライン)と、棚の combo の配線を通らない押し出しの試験を手当てした。
  - 画面寸法 / 拡大率 … 3 サイズ × 5 場面 × 3 倍率を撮り、目視した。**Windows の表示倍率そのものを変えた確認だけは未達**(検証機が 150% 固定。倍率は QT_SCALE_FACTOR で代用)。
  - 古い重複 UI の片づけ … 窓 2 つ(配列・数値)を削除、形状ガイド(旧)を献立から削除、出力モードの右を 1 枚に。残るのは製作モードの `{Fabrication, Parameter}` 1 組だけで、理由は ShelfLayout.cpp と matrix C-09 に書いてある。
  - PC 検証 … 6・7・8 回目とも全部緑。

### 2026-09-20 右ペインを全部 1 枚に(C-09 の最後の 1 組)
- 製作モードの `{Fabrication, Parameter}` を `{Fabrication}` に。数の棚には自分の入口 `view.number_settings`(「数の設定」、表示メニュー)を与えた。表示設定(`view.display_settings`)と同じ形。
- これで **どのモード・道具の組み合わせでも右は 1 枚**。自分の命令で出す棚は 作業平面 / 表示 / 役割表 / 配列 / 型紙 / 数 の 6 つで、棚の到達性の関所に並べてある。
- 人の道: RG-09「右は1枚で、数の棚は『数の設定』でだけ前に出る」を追加(4 モードで並ばないこと、命令で出ること、道具を持つと引っ込むこと)。

### 2026-09-20 PC 検証 9 回目(d5fed10)= 新しい命令の配線漏れを 1 件
- 自己試験 295/297。落ちたのは新設した「数の設定」の 2 件だけ:「未接続のコマンドが1つも無い」と RG-09。原因は `V2MainWindow::IsViewCommand` の id の並びに `view.number_settings` を足し忘れたこと(台帳・献立・説明書・棚の配線は入れてあったが、命令の振り分けの入口だけ抜けていた)。→ 足した。
- **この関所が効いた**: 台帳へ足して振り分けを忘れると「押せるのに何も起きない」になるが、`未接続のコマンドが1つも無い` が拾ってくれる。新しい命令を足すときは 台帳 / 振り分け(Is*Command) / 献立 / 説明書 / モードの一覧 の 5 か所と覚える。

### 2026-09-20 PC 検証 10 回目(3ec0d93)= **統合段は全部緑で完了**
- ctest **157/157**、自己試験 **297/297**。右ペインはどのモード・道具の組み合わせでも 1 枚(C-09 完了)。
- 統合段(人の道の回帰 / 画面寸法・拡大率 / 古い重複 UI の片づけ / PC 検証)はここで閉じる。**未達として残すのは 1 つだけ**: Windows の表示倍率そのものを 100%/125% にした撮影(検証機が 150% 固定。倍率は QT_SCALE_FACTOR で代用し、1280x720〜2560x1440 の絵は目視済み)。

### 2026-09-21 オーナー報告の 4 件(実機で見つかった本物の不具合)
1. **「スナップしてるのに 90 度になっていない」** — 点の吸着(端点・グリッド)はあったが、**向きの吸着が無かった**。1px 狙いを外すと -89.95 度の線が黙って文書へ入る。→ `SnapDirectionToRightAngle`(core、許し 1.5 度)を足し、**点の吸着先が無い自由な点のときだけ** 長さを変えずに直角へ寄せる。Shift の固定は今までどおり。S や「スナップ」を切っているときは寄せない。試験: core `constraint / ほぼ直角の向きは直角へ寄る`、人の道 `HP-AC-01`。
2. **「長さと角度を指定しても線が引けない」** — 原因は 2 つ。(a) **Tab が打った字を捨てていた**(`FocusNextField` が焦点を動かすだけ)。案内どおり「長さを打ち Tab で角度へ移って Enter」と操作すると長さが消え、角度だけでは形が決まらないので何も起きなかった。→ `LockAndFocusNextField`(打った値を確かめて留めてから移る。空の欄は素通り、読めない値は移らずに理由を出す)。(b) **角度の欄に単位なしで打った数がラジアン扱い**だった。90 と打つと 90 ラジアン(約 5156 度)。欄の表示は度なので、打つほうも度で読む(`rad` と書いたときだけラジアン)。試験: core `cursor_input / Tabは打った値を留めてから隣の欄へ移る`、`expression / 角度の欄の単位なしの数は度として読む`、人の道 `HP-AC-02`。
4. **「ほかの線の延長線だとグリッドが反応しない」** — 吸着の順位で **延長線がグリッドより強かった**(SnapEngine.h に「暫定・オーナー判断待ち」と書いてあった置き場所)。格子を狙っているのに、近くの線の延長と重なっただけで格子から外れた点が入る。→ 延長線は「線」の案内、グリッドは「点」。点を採る(延長線をグリッドの下、自由点の上へ)。試験: core `snap / 延長線と重なってもグリッドの点を採る` と順位の固定表。
3. **「拡大したときにグリッドが消えている箇所がある」** — グリッドを **原点を中心にした決まった大きさの塊** で引いていたため、原点から離れて拡大すると画面の端から消えていた。→ 画面の四隅を作業平面へ戻して見えている範囲を測り(`VisibleGridRangeUV`)、その範囲のぶんだけ引く(`GridLineRangeFor`、core)。本数の上限は安全弁として残す。試験: core `grid / 見えている範囲のぶんだけ線を引く`。

### 2026-09-21 PC 検証(4547879)= オーナー報告 4 件の手当てを実機で確認
- ctest **157/157**、自己試験 **299/299**。HP-AC-01(ほぼ真下の線が真下ちょうど)と HP-AC-02(長さ → Tab → 角度 → Enter で打ったとおりの線)が実機で通った。
- 途中の 1 回は、試験が `CommitCursorField` だけを呼んで点を置く道を通っていなかったので落ちた。入力列の Enter を `PressEnterInCursorInput` の 1 本にまとめ、鍵盤も試験もそこを通すようにした。
- グリッドの描き方(見えている範囲だけ引く)と、延長線よりグリッドを採る順位は core の試験で固定。実機の目視はオーナーの次の操作で。

### 2026-09-21 オーナー報告 5 件目: 「直線上なのに曲線上って出てる。線上のグリッドにスナップできない」
- **言葉**: 最近点のラベルが「曲線上」だった。直線にも円にも出るので「線上」にした。
- **吸着**: 最近点(順位 3)がグリッド(6)より強く、しかも最近点はポインタ直下に付いて距離がほぼ 0。線の上ではグリッドの点が永遠に選ばれなかった。→ 新しい種類 **線上の格子**(`SnapKind::GridOnCurve`)を足した。線がグリッドの線を横切る点(直線は解析的、ほかの曲線は格子点が線に乗るとき)で、順位は中点・中心の下、最近点の上。格子から離れたところは今までどおり最近点。
- 試験: core `snap / 直線の上でもグリッドの点に吸着する`、`斜めの線ではグリッドの線を横切る点に吸着する`、`格子から離れた線の上では最近点のまま`、順位の固定表。人の道 `HP-AC-03`。

### 2026-09-21 PC 検証(ac58122)= 線上のグリッド吸着を実機で確認
- ctest **157/157**、自己試験 **300/300**。HP-AC-03(既存の直線の上の格子点へ線の端がちょうど乗る)が実機で通った。

### 2026-09-22 オーナー方針「人が引くワイヤーは、面が必ずそこを通る線である」
- 報告: 外周と、内側に引いた円弧(膨らみの目安)/縦の線をまとめて境界面へ入れると、GEO-W002(枝分かれ)や GEO-G004(閉じていない)で止まった。境界面は「選んだ線を全部 1 本の輪につなぐ」ことしかできず、内側の線を受ける口が無かった(形を作る核 BRepOffsetAPI_MakeFilling は受けられるのに、つないでいなかった)。
- 手当て:
  - `app/OuterLoopSplit`(core):選んだ線を端点でグラフにし、行き止まり(T 字で辺の途中に当たる線など)を外し、閉じた輪を全部数えて **囲む面積(Newell)が最大の輪を外周**、残りを **通る線** にする。輪が無ければ UI-R011、多すぎれば UI-R012 で理由を言う。
  - 境界面の役割に GuideU = **通る線**(任意の欄 `SurfaceSlotState::Optional`)。欄に直接入れてもよいし、境界に全部入れても自動で分ける。
  - 核: 通る線を `MakeFilling::Add(edge, C0, IsBound=false)` で拘束。**作ったあとで全部の線(外周も通る線も)との距離を測り、許容を超えたら面を捨てる**(既存の MeasureDeviation)。できないことをできたことにしない。
- 試験: core `outer_loop / 外周6本と上下の頂点を結ぶ円弧は外周と通る線に分かれる`、`外周の辺の途中にT字で当たる線は通る線になる`、`閉じた輪が無ければ理由を言う`、`境界面の表は外周1行と通る線の行になる`。kernel `kernel_fill / 外周の内側に引いた線を面が必ず通る`(PC)。人の道 `HP-SF-10`(PC)。

### 2026-09-22 PC 検証(6b604f3)= 境界面の「通る線」を実機で確認
- ctest **157/157**(kernel `外周の内側に引いた線を面が必ず通る` を含む)、自己試験 **301/301**(HP-SF-10 を含む)。

### 2026-09-22〜23 自由曲面と入力の数(指示書 `kachakachaCAD_Opus5_surface_multiselect_implementation_prompt.md`)
順番はオーナー指示どおり P1→P2→P3→P4 → P5→P6→P7 → 部品・製作の入力の数 → P8(台帳)→ 統合/PC。

| 段 | 内容 | commit | 雲 | PC |
|---|---|---|---|---|
| P1 ロフト基盤 | 断面 1〜任意 + ガイド 0〜任意 + 中心線 0〜1(3 本目以降も全部拘束)、個数の約束を 1 か所(SurfaceCardinality)、四辺面の入口 | f879333 | loft_input_tests 12 | 38b3184 以降の kernel_loft_tests 通過 |
| P2 選んだ線の一覧 | 欄を可変長の一覧(× で外す・向き反転・手動の並び)、中心線の欄、四辺面の張り方、作り方の内訳、一括(離した面・回転体) | 70cfb9c | — | HP-SF-06/11/12 通過 |
| P3 連続条件 | G0/G1/G2 と支持面(境界面・四辺面の辺ごと)、作ったあと自分で測る、保存 | 819b8f2 | guide_surface_tests、feature_definition_tests | kernel の連続条件通過 |
| P4 曲線網 | 厳密(Gordon: S=L_U+L_V−T)と近似(Filling)を別の作り方に、成り立つ方を言う | 0173810 | gordon_grid_tests | 通過(PC で核の不具合 3 件を見つけて直した) |
| P5 面の編集 | 合わせる・つなぐ・整える・対称・U/V 線・面へ投影(新しい Feature EditSurface) | 5dd930f | surface_edit_input_state_tests、curve_fit_tests | 66d3cac で全部通過(つなぐの G1 を直に組む形へ直した) |
| P6 面の解析 | ゼブラ・曲率・U/V 線・コーム・境目・入力線からのずれ・製作性の目安(数値の基準つき) | 57bb941 | surface_analysis_tests 7 | HP-SA-01/02 通過 |
| P7 おまかせ | 押した線の役割と作り方をつながりから決め、理由・他の候補、役割の色、線ごとに直せる | b3853ee | surface_role_assist_tests 12 | 66d3cac で HP-SF-13 通過 |
| 部品・製作の入力の数 | 足す・引く(相手何個でも)、厚み(面の枚数だけ)、角・間隔(線ごと)、移動・複写(1 回で戻る)、保存した線の編集の読み直し / 押し出し(部品ごとの輪郭)、かご(シェルごと)、回転体・治具・面を平面まで・固定・書き出し(選んだもの全部) | 26f00d3 / 818113d | ctest 159/159、typecheck 123 files | 26f00d3 まで PC 通過(66d3cac)。818113d 以降は次の PC で |
| P8 台帳 | UI_FEATURE_MATRIX(D-26〜D-42 と 入力の数)、TASK_LEDGER、UI_LESSONS、acceptance-coverage | この commit | — | — |

- PC の結果(雲で `_claudeout` を読んだ):
  - 38b3184: ctest 161/165、自己試験 306/307。落ちたもの(双曲線を近似で写していた・面を合わせるの G1 13.3 度・狭い画面で右の棚が窓を押し広げる・Geom_BSplineCurve の不完全型)を b3853ee で手当て。
  - b3853ee: ctest 164/166、自己試験 307/308。つなぐの張り 1.8 で縁 A が 3.2 度折れる(埋める面の近似)、HP-SF-13 で 5 本押しても外周 4 本しか残らない(道具を先に押すと作り方が平面になり、縁の線を押すたびに輪郭がまとめて入ったり外れたりした)。→ 66d3cac: 面を作るでは線の上を押したら線 1 本、線の無い内側だけ輪郭をまとめて拾う。つなぐは面を直に組んで G1 を作りで満たす(G2 の側があるときだけ埋め直す)。
  - **66d3cac: ctest 165/166、自己試験 309/309**。落ちた 1 件は試験の不具合(文書の並びへ足したあとで、足す前に取った参照を読んでいた。MSVC は並びの伸ばし方が違うので PC だけで壊れた形を読んだ)→ 形を先に写すよう直した。
- 818113d(雲のみ): 押し出しは部品ごとに自分の輪郭を記録(作り直すと 2 個目以降が 1 個目の写しになっていた。古い文書は作り直しの段取りが「同じ定義の何番目か」を持って配る)。かごはシェルごと。回転体は最後の直線が軸・ほかは全部断面。治具・面を平面まで・固定・書き出しは選んだもの全部を 1 回で。選択の条件に「1 つ以上」を 4 つ足し、使われなくなった「ちょうど 1 つ」は試験の「まだ使っていない条件」へ理由つきで移した。
- 残り(未達として正直に残すもの): 違う平面の輪郭を別々の押し出しにする、部材の範囲結合・K 分割、立体の辺のフィレット・シェル(核に無い)、画面の目視(PC_VERIFIED ではなく PC_TESTED)。

## BLOCKED_BACKEND(必要なら別担当へ)

| ID | 何が要るか | 入力 / 出力 | 既存の近い API | 無い理由 |
|---|---|---|---|---|
| D-04 | 3点円 | 3 点 → Circle segment | `ArcBuilders.h` 3点円弧 | `CurveSegment::MakeCircle` に3点版なし |
| D-08 | 中心・始点・終点の円弧 | 中心/始点/終点 → Arc | `ArcFromEndpointsAndRadius` | ArcMode に無い |
| D-13 | スプライン 通過点/Fit、次数、連続性、許容差、重み、接線 | 点列 + 条件 → BSpline | `MakeCubicBSpline`(制御点のみ) | 補間器なし |
| D-14 | 楕円 | — | — | CurveKind に無い |
| D-22 | スケール | 線 + 中心 + 倍率 | `TranslateCurve` 等 | `WireTransformMethod` に無い |
| D-34 | テキスト注記 | — | ReferenceDimension のみ | entity 種なし |
| C-15 | 面積 | 閉じた輪郭 → mm² | `SignedAreaOnPlane`(押し出し用、内部) | Measurement に無い(押し出しの内部関数を公開すれば可) |
| P-03 | 押し出し From(開始面) | 開始平面 → startOffset | `startOffsetMm`(内部) | Request/Choice に欄なし |
| P-07 | テーパー | 角度 | — | Prism のみ |
| P-08/09 | Solid 回転体/ロフト/スイープ | — | 面版 + 厚み | ThruSections solid=false |
| P-12/13 | Solid フィレット/面取り/シェル/分割 | — | — | TKFillet 未リンク |
| P-15 | 面オフセット/削除/置換 | — | — | なし |
| P-17 | 交差 | — | `BuildBoolean` Union/Difference | Common なし |
| P-18 | Part の移動/回転/ミラー/コピー/パターン | — | 線の変形 | TransformPart なし |
| F-03 | 平均誤差、部材ごとの方式(Plane/Cyl/Cone) | — | `CurvedPanelResult::analysis`(捨てている)、`ApproximatedBand::estimatedDeviationMm` | 集計と保持が無い(小改修で出せる) |
| F-04 | 誤差ヒートマップ | — | — | なし |
| F-06 | 分割の位置指定 | rail parameter | `PreviewBandSplit`(中央固定) | 引数なし(小改修) |
| F-08 | 曲面部材の切れ目 | — | — | 明示的に未実装 |
| F-11 展開先(作業面/XY) | 展開先を XY平面/現在の作業面/新しい作業面 にする | 近似モデル + 展開先の指定 → その平面上の展開曲線 | `LayoutPattern`/`PlacePanelCurves`(紙 A4 固定) | 展開先は紙(PaperSize)固定で、平面や作業面を渡す引数がない |
| F-12 表裏反転 | 展開した型紙を裏返す | 近似モデル(+反転の指定) → 鏡像の展開曲線 | `PlacePanelCurves`(向きは1通りのみ) | placement/LayoutPattern に mirror・反転のフラグがない |
