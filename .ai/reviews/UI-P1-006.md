PASS

I found nothing that blocks this stage. I only read files and changed nothing. The review covered CURRENT_TASK, spec §3, the whole diff, the four untracked files, the earlier REVISE notes and the attempt-3 gate logs.

**Evidence**
- **Gates ran on this code.** The worker report's "not run" is out of date. The attempt-3 build log is from 06:02:19, after the last source edit (05:59:43).
  - CTest passed 129/129.
  - `--self-test` passed 168/168, including all six semantic-state cases and the new Preview pen case.
  - `V2SelfTestSemantics.cpp` matches the reviewed SHA-256, and `git diff --stat` matches the evidence file.
  - Both build warnings (`V2Viewport.cpp:1119`, `V2ViewportDraw.cpp:537`) are in lines this diff did not change.
- **Last revision request is fixed.** All four Preview pens now use `V2Viewport::PreviewPen()` in `V2ViewportDraw.cpp`: the drawing preview, the body-drag ghost, the control-point ghost and the fold preview. The pen uses the Preview width, a dashed line and alpha 0.75. `CasePreviewPenIsFainterThanConfirmedLines` checks it at the default width and at 0.25px, so spec §3 rule 3 holds.
- **Acceptance tests:**
  - **Separate states:** the state is decided in `src/next/.../SemanticState.cpp`, which has no Qt or OCCT. Colours and widths come only from `SemanticColor` and `SemanticWidthPx`. Selected beats Hover, and Hover never changes the selection; both are tested in core and in the self-test.
  - **Only the selected segment is highlighted:** one rule, `RefCoversCurve` in `Selection.cpp`, feeds `IsCurveSelected`, `SelectedCurves`, `CurveSemanticState` and `ControlPointsForSelection`. Line colour, grab handles and command targets therefore can't disagree.
  - **Theme change:** I recomputed the colour distances. Every pair of states, and each state against construction lines, is at least 89 apart in the standard theme and at least 128 apart in Windows 95 (the gate is 64). The theme switch replaces the whole palette. `ApplyDisplayChoice` changes only the wire, construction and background colours.
  - **Preview can't be selected:** it lives only in `hover_.preview`, never in the scene or document. It is cleared on tool change, on cancel and after a successful finish, and kept when finishing fails.
- **Data, history and lifetime:**
  - Moving by drag works on `Selection().entityIds`, so drawing the ghost for the whole object matches what actually moves.
  - A control-point edit (`ReplaceWireSegment`) and a numeric edit (`V2EditCommands.cpp:275`) both reselect the whole object afterwards.
  - Every code path that creates a wire stores its segment IDs, so segment IDs don't change when the scene is rebuilt.
  - Nothing is added to the saved file or the undo history. Nothing new is under `src/next` Qt/OCCT, and V1 is untouched.

**Not blocking**
1. **Undo can leave a hidden selection.** `PruneSelection` in `Selection.cpp:602` only removes entities that no longer exist. Suppose a numeric edit changes a wire's segment count, the user then selects one segment, and presses Undo. The old segment IDs come back, but the selection still points at the new one. The wire then shows no highlight and no handles, yet it is still selected and Delete or Move still acts on it. `SelectedCurves` already had this gap before this diff. A later package should also drop segment references whose segment is gone.
2. **Custom wire colour isn't checked.** A wire colour picked in the display settings could be too close to the Hover or Selected colour. The acceptance test only covers theme changes.
3. **Fold preview needs a look on screen.** It changed from a solid line to a thin, semi-transparent dashed one, and the owner should check it is still easy to read.
