# 引継ぎ書(2026-09-11)── kachakachaCAD V2 の作業を別の担当(ChatGPT 等)へ渡すためのもの

この文書は、いまの担当(Claude)が5時間制限で止まったとき、次の担当がそのまま続けられるように書く。
**上から順に読めば、いま何が動いていて、何が途中で、次に何をするかが分かる。**
短い版(チャットへ貼る用)は末尾 §9。

---

## 1. リポジトリと環境

- GitHub: `https://github.com/tatata93/kachakachaCAD.git`
- 作業ブランチ: `v2-wp01`(雲側)。PC 側は `codex/v2-wp01-build-scaffold` へ push している(`_FIX_AND_BUILD.cmd` の [6])。
- PC の作業場所: `C:\Users\tak01\github\kachakachaCAD`
- ビルド: CMake preset `windows-msvc`(MSVC 2022 x64, Qt 6.9.2, OCCT 8.0.1, C++20)
  - `cmake --preset windows-msvc` → `cmake --build --preset windows-msvc --parallel` → `ctest --preset windows-msvc`
  - 実行ファイル: `C:\Users\tak01\github\kachakachaCAD\build-msvc2022-x64\Release\kachakacha_cad_next.exe`
  - 自己試験: `kachakacha_cad_next.exe --self-test`(全ケースを窓を出して回す。落ちた場所は `RUN 名前` の直後に出る)
  - 手順書の撮影: `kachakacha_cad_next.exe --manual-state <名前> --snapshot <png>`
- PC 側の自動ループ: `_WATCH.cmd` が `to-pc.bundle`(git bundle)を待ち、`_FIX_AND_BUILD.cmd` で fetch → build → ctest → 撮影 → 自己試験 → push → アプリ起動。結果は `_claudeout\run.txt`(= `latest.txt`)、画面は `_claudeout\v2-*.png`。
  - 雲(ネット制限で GitHub へ push できない)からは `git bundle create to-pc.bundle v2-wp01 --not <PC にある最新コミット>` を作って PC の `to-pc.bundle` へ置く。
  - **PC で直接作業する担当なら bundle は不要**。普通に編集 → build → ctest → 自己試験 → commit → push でよい。

## 2. 構成(どこに何があるか)

| 場所 | 中身 | 決まり |
| --- | --- | --- |
| `src/next/kachakacha/` | V2 の core(Qt/OCCT を含まない)。`base/ geometry/ modeling/ domain/ document/ io/ app/ fabrication/ view/` | **Qt と OCCT を include しない**(試験で捕まる) |
| `src/next_occt/kachakacha/kernel/` | OCCT を使う核(押し出し・厚み・面) | 雲では compile できないので変更は小さく |
| `src/apps/cad_next/` | Qt の画面。`V2MainWindow.*` が窓、コマンド群は `V2*Commands.cpp` に分割、棚(右パネル)は `V2*Dock.*`、自己試験は `V2SelfTest*.cpp` | 判断は core に置き、画面は「集めて渡す・出す」だけ |
| `src/apps/cad/`, `src/core/` | **V1**(旧版)。`examples/railway-nose-acceptance.kcd` は V1 の保存形式 | **消してはならない**(オーナー指示: V2 が満足いくまで V1 を消すな) |
| `tests_v2/` | core の試験(`KACHA_V2_TEST(suite, name)` / `KACHA_V2_TEST_MAIN`、`Require/RequireEqual/RequireNear`) | ctest で全部回る |
| `docs/v2/` | 台帳と契約。`command-catalog.md`(コマンド台帳)、`diagnostic-catalog.md`(理由番号台帳)、`acceptance-coverage.md`(受入試験の対応表)、`core-ui-wiring.md`(core→画面の結線)、`docs/manual/作り方.md`(手順書) | **台帳は試験で照合される**。コマンド/理由番号/手順表を足したら台帳も直す |
| `tools/qtstub/` | Qt の当て木。`sh tools/qtstub/typecheck.sh . g++` で `src/apps/cad_next/*.cpp` を `-fsyntax-only` 型検査(Qt が無い環境用) | 新しい Qt API を使ったら当て木にも足す |

守る門(試験で落ちる): 1 ファイル 1500 行以下、1 関数 100 行以下、core に Qt/OCCT なし、台帳の照合(コマンド・理由番号・手順表・結線)。

## 3. オーナーの指示(効力が続いているもの)

1. 「すべての権限を与えるので全自動でやれ。いちいち承認を求めるな」── 聞かずに進める。
2. 変更のたびに PC で build → 全試験緑 → 自己試験緑 → 動作確認。**未 push のコミットを溜めない。**
3. コミット末尾に付ける行(Claude 担当のとき):
   `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_01PhuCKP4297isBKknmQumJ3`
   (別の担当なら、その担当の署名でよい)
4. 実装が終わったらソフトを起動し、.exe のパス(§1)を示す。
5. **V1 は消さない**(WP-13 保留)。
6. 公理「できないことを、できたことにしない」── 出来ないときは理由番号(`XXX-N000` 形式、`docs/v2/diagnostic-catalog.md` に登録)を付けて断る。黙って補わない。
7. 想定ワークフローは4工程: 作図(ワイヤ)→ 押し出し・立体化 → 近似(曲げ具合は可変、V1方式とV2方式を両方選べる)→ 出力。core にあって画面から使えない機能を残さない(`core-ui-wiring.md` で機械照合)。
8. 直近の指示(2026-09-10 夜): **作図系は V1 で完成に近いので V1 を全面移植しろ。** 具体的には
   (a) 左一覧に V1 の origin 面が無い → 原点面と原点軸を出す、(b) 作業平面が作れない → V1 の多数の作り方を移植、(c) 作図の線が描画中も後も点線なのはおかしい、円弧・円の作り方が選べない、数値指定がおかしい、(d) グリッドの主線・副線設定、(e) 上部バーに常に「選択」「測定」を出しどの場面でも使える(V1 と同じ)、(f) `examples/railway-nose-acceptance.kcd` を kcd2 で使えるように。
   走査結果と移植計画: **`docs/v2/v1-port-survey-2026-09-11.md`**(完全移植/部分移植/現状の不足/順番 §5)。**この §5 の順に進める。**
9. 「先ほど途中だった作業も並行して続けろ」── 移植と並行して、V2方式の部材分割(PanelStrategy/ReliefCut、核の面標本が要る)・V2 曲げ状態(`Assembly.h`)・`OpeningClip.h` の結線も残っている(§6)。
10. 調査に Agent(サブエージェント)を使うのをオーナーは断った。自分で読む。
11. セキュリティ: ネットからの取得は許された道具(WebFetch/WebSearch)だけ。bash/curl/python で URL を取らない。

## 4. いままでに済んだこと(全部 PC 緑・push 済み、コミット `eb42e56` まで)

- 4工程の結線、押し出し選択肢、面に厚み(`part.thicken` / `part.thicken_to_plane` / `part.thickness_placement`)、作業平面(核の作り方 11 通り)。
- 近似: V1方式(帯近似 `fabrication/Band*`)と V2方式(`ClassifyFaces`)を `fabrication.set_method` で切替。組立率 `fabrication.set_assembly`、任意状態の固定 `fabrication.freeze_state`、接続部分 `fabrication.set_connection_scope`。近似モデルは文書に保存・復元。
- 形状ガイドの役割表を人が組む(`guide.*` 9 コマンド、`app/GuideTableBuild`)。
- 選んだものだけの別 kcd2(`app/SubDocument`、EXP-S001〜S003)。
- 実形状(ER 前頭部 1/87)の受入試験 `tests_v2/acceptance_er_tests.cpp`、曲面の窓(`app/FabricationOpenings`)、曲面へ投影 `wire.project_surface`。
- 矩形・円・円弧が XY 決め打ちだった欠陥を修正(`ToolSession::SetPlane`)。
- 台帳: `docs/v2/v1-port-survey-2026-09-11.md`(走査)。

## 5. いまの状態(2026-09-11 更新)

**PC で緑**: 移植計画 §5 の (2) 原点ノード + 作業平面の棚、(3) 作図の右パネル、
(4) グリッド設定 + 表示設定、(5) 上の帯(正対・選択・測定・作図面・作業中グループ・吸着)と
4モードそれぞれの2段目の道具列。線の太さは V1 の既定に合わせた。

すでに入っている作図側の内容:
- 作図中に数値入力列(CursorInput)を本当に開き、Enter で打った値のまま点を置く
  (`DrawingSession::PlacePoint`、`V2Viewport::SyncCursorInputWithTool / PlacePointFromCursorInput`)。
- 作図の棚 `V2DrawingDock`(円弧の作り方 3通り・半径・中心角・補助線として作図・指定した点を
  作図点として残す・数値で線を作る)。core: `ToolSettings.keepPoints`、矩形の実寸欄(幅・高さ)、
  `app/DirectWireEntry`(UI-D001/D002)、`DrawingSession::AddWire`。
- 自己試験: `V2SelfTestInput.cpp`(入力列)、`V2SelfTestDrawing.cpp`(棚3件)。

落ちていたら `_claudeout\run.txt` の `RUN` 直後を見る。よく落ちる所: 自己試験のクリック位置
(mm で置くこと)、入力列が開いたまま次の操作をしている所(`SyncCursorInputWithTool` が閉じる条件)。

**2026-09-11 06:00 追記(Claude)**: ChatGPT の ccfc825(上の帯・モード別道具列)を土台にした。
2段目の並びは台帳(`app/UiMode` の `CommandIdsForMode`)から作るよう変えた(別の一覧を持たない)。
その上に **d92610d**: オフセット(`wire.offset`、数の棚「オフセット距離」)・2線を交点まで
(`wire.meet_lines`)・交点に点(`wire.intersection_points`、`app/IntersectionPoints`、UI-X101)・
基準線に設定/解除(`wire.set_datum / clear_datum`、一点鎖線)。**PC 確認中(束を送った)**。
一致解除・滑らか解除は V2 に拘束が無いので対象外(手順書 3-7 に明記)。

**2026-09-11 08:10 追記**: さらに **7a9dedc** 角の加工(`wire.corner_chamfer / corner_fillet`、
`geometry/PolylineCorners`)、**b784ad5** V1 の .kcd 読み込み(`io/KcdImport`、「開く」で
`railway-nose-acceptance.kcd` が開ける。読めない命令は KCD1-I002 で名前を挙げる)。
**PC は ChatGPT の未コミット分(同じ (6) の作業)があって束を merge できていない。**
PC で `git stash` → 束を取り直す(または ChatGPT に commit/push させて私が乗せ直す)。オーナーへ選択を出した。

**2026-09-11 08:30 追記**: **4497905** 測定の3モード(2点間・3点角度・要素、`app/MeasurePanel`、
`V2MeasureDock`、「寸法を残す」= `AddReferenceDimensionCommand`、UI-M001)。
PC の詰まりは自動で解くことにした: `_FIX_AND_BUILD.cmd` の [1] で、未コミットの編集があれば
`git stash` → その stash を `local-wip-日時` ブランチにして origin へ push(消さない)→ 束を ff-merge。
ChatGPT の分は `origin/local-wip-*` に残るので、要るものがあれば後でそこから拾う。

**残り(§5 の順)**: (6) の残り = 編集タブ(選んだものの数値編集)・面取りの欄。
(9) 部分移植(回転体・製作パネル・材料/範囲/積層)。

**二人で触るときの決まり**: 同じ段を二人で作らない。作る前に `git fetch origin` で
`codex/v2-wp01-build-scaffold` を見て、着手する段を引継ぎ書 §5 に一行書いてから始める。

## 6. そのあとの順番(走査 §5、上から順に。各段で PC 緑・push)

1. ~~(1) 点線の原因確認~~ ── 雲の撮影では再現しない。オーナー環境のスクリーンショットが要る(未着手。V1 の線の描き方は `CadViewport.cpp`、V2 は `V2ViewportDraw.cpp` の QPen。`Qt::DashLine` が補助線以外に使われていないか、`DisplayStage` の薄い表示が「作図面以外の線」に効いて点線に見えていないかを疑う)。
2. (2) 原点ノード + 作業平面の棚 + 作図面コンボ + 正対 ── **済(PC 緑・push 済み)**。
3. (3) 作図の右パネル ── **済(PC 緑・push 済み)**。§5 参照。
4. (4) グリッド(主点間隔・副点・基準 X/Y・色・表示条件)+ 表示設定(線の色/幅/様式、設計/完成形/選択だけ、Ctrl+2/3) ── **済(PC 緑・push 済み)**。
5. (5) 上部バー: モード + 正対 + 選択 + 測定 + 作図面 + グループ + スナップ。2段目はモードごとの道具列 ── **済(PC 緑)**。
6. (6) 無い道具 ── オフセット・2線を交点まで・交点に点・基準線に設定/解除は **済(d92610d、PC 確認中)**。一致解除・滑らか解除は対象外。**残り: 角の加工・編集タブ・面取りの欄。**
7. (7) 測定の 3 モード(2点/3点角度/要素)+ 寸法を残す/測定を消去 ── **済(4497905、PC 確認待ち)**。
8. (8) `.kcd` の読み込み ── **済(b784ad5、PC 確認待ち)**。読めるもの/読み飛ばすものは手順書 9 と `io/KcdImport.h` の頭に一覧。
9. (9) 部分移植: 回転体、製作パネル、材料/範囲/積層。
10. 並行して残っているもの: V2方式の部材分割(PanelStrategy 1枚/少数/完全/混合、ReliefCut)は核に「部品の面ごとの標本と隣接」を取る道(`SamplePartFaces` 相当)が無いので未接続。V2 曲げ状態(`fabrication/Assembly.h`, `FreezeState.h`)未使用。`fabrication/OpeningClip.h`(平らな部材をまたぐ窓)未接続。回転体・押し出し面。

## 7. 落とし穴(過去に踏んだもの)

- 文書へ足したあとに Entity/定義へのポインタを使い続けて落ちた(`std::vector` の再配置)。**文書を変える前に写す。**
- `fabrication.freeze_state` が接頭辞 `fabrication.` で先に拾われ届かなかった → `IsFabricationCommand` は freeze を除く。
- `RebuildKernelShapes` の早期 return で役割表の更新が飛んだ。
- 矩形・円・円弧が XY 決め打ちで ZX 面に描けなかった → `ToolSession::SetPlane`。
- 自己試験の座標は画面比で置くと窓の大きさで落ちる。**mm で置く。**
- 台帳の理由番号は `src/next` と `src/next_occt` だけが走査される。画面(`src/apps`)で作った番号は台帳試験に載らないので、**理由は core で作る。**
- 1500 行の門: `DocumentFile.cpp`、`V2SelfTestModeling.cpp`、`V2MainWindow.cpp` は分割済み。太ってきたら分ける。
- 名前 `origin`(Vector3)と衝突するので原点面の印は `isOriginPlane`。

## 8. 進捗の記録場所

- Claude プロジェクトの文書 `claude/V2実装-作業状態.md`(次の担当が Claude でないなら、この引継ぎ書と `git log` が正)。
- リポジトリ: `docs/v2/v1-port-survey-2026-09-11.md`(計画)、この文書、`docs/v2/acceptance-coverage.md`。

## 9. チャットへ貼る短い版

```
kachakachaCAD(C++20/Qt6.9.2/OCCT8.0.1/CMake)の V2 実装を引き継いでください。
リポジトリ: https://github.com/tatata93/kachakachaCAD.git  ブランチ v2-wp01(PC は codex/v2-wp01-build-scaffold へ push)
PC: C:\Users\tak01\github\kachakachaCAD  build: cmake --preset windows-msvc / cmake --build --preset windows-msvc --parallel / ctest --preset windows-msvc
自己試験: build-msvc2022-x64\Release\kachakacha_cad_next.exe --self-test
詳しい引継ぎ書: docs/v2/handover-2026-09-11.md、計画: docs/v2/v1-port-survey-2026-09-11.md(§5 の順に進める)

守ること: 承認を求めず全自動で進める / 変更ごとに build→ctest→自己試験を緑にして push(溜めない)/ 終わったらアプリを起動し .exe パスを示す / V1(src/apps/cad, src/core)は消さない / できないことをできたことにしない(理由番号を付けて断る。番号は core で作り docs/v2/diagnostic-catalog.md に登録)/ core(src/next)に Qt・OCCT を入れない / 1ファイル1500行・1関数100行以下 / コマンド・理由番号・手順表・結線の台帳は試験で照合されるので一緒に直す / ネット取得は許された道具だけ。

いまの作業: V1 の作図系を全面移植する計画(survey §5)。(2)〜(5)(原点ノード・作業平面の棚・作図の棚・グリッド/表示の棚・上の帯とモード別道具列)は PC 緑・push 済み(ChatGPT の ccfc825 まで)。(6) の不足道具のうち オフセット・2線を交点まで・交点に点・基準線に設定/解除 は d92610d でコミットし PC の build/ctest/自己試験を確認中(落ちていれば _claudeout\run.txt の RUN 直後を見て直す)。同じ段を二人で作らないこと(着手前に git fetch origin で codex/v2-wp01-build-scaffold を見る)。
次: (6)の残り(編集タブ=選んだものの数値編集、面取りの欄)→(9)回転体など部分移植。角の加工(7a9dedc)・.kcd 読み込み(b784ad5)・測定3モード(4497905)はコミット済み・PC 確認待ち。PC 側の未コミット編集は _FIX_AND_BUILD.cmd が local-wip-日時 ブランチへ退避して push する(消えない)。並行: V2方式の部材分割(PanelStrategy/ReliefCut、核の面標本が必要)、V2 曲げ状態、OpeningClip の結線。未解決: 作図線が点線に見える原因(雲では再現せず、オーナー環境の画面が要る)。
```
