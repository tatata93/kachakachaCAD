# V1の作図機能との同等性(オーナー指示、必須)

**オーナー指示 2026-09-08: 「作図系の機能はv1でできたことと同等なことができる必要がある」**

この表はV1の実装から機械的に洗い出したものであり、**V2の完了条件**とする。
「V2の仕様書に書かれていない」ことは、落としてよい理由にならない。
落とすものがある場合は `product-contract.md` §14(非目標)か §15(切替後の追加候補)へ
**明示的に書き、オーナーの確認を取る**こと。黙って消してはならない。

出典: `src/apps/cad/CadViewport.h` の `ViewportTool` / `DrawingSnapKind` /
`ArcDrawingMode` / `MeasurementMode`、`src/apps/cad/MainWindow.cpp:755-802` のQAction定義。

## 1. 作図ツール(23種)

`DrawingTool` は V1 の `ViewportTool` と1対1に対応する。
状態機械は `src/next/kachakacha/modeling/ToolController.*`、試験は `tests_v2/tool_tests.cpp`。
「操作の流れ=済」は、点の受け取り・取り消し・中断・途中プレビューが動くこと。
「Qt=未」は、それを画面へ繋ぐ部分がまだであること(WP-08 後半)。

| # | V1 | V2での担当 | 状態 |
| --- | --- | --- | --- |
| 1 | 選択 | WP-08 | 操作の流れ=済 / Qt=未 |
| 2 | 画面で基準を合わせる(グリッド原点) | WP-08 | **操作の流れ=済** / Qt=未 |
| 3 | 作図点 | WP-08 | **操作の流れ=済** / Qt=未 |
| 4 | 直線 | WP-04/08 | **曲線=済 / 操作の流れ=済** |
| 5 | ポリライン | WP-04/08 | **曲線=済 / 操作の流れ=済** |
| 6 | 矩形 | WP-08 | **操作の流れ=済**(対角2点→4本) |
| 7 | 円 | WP-04/08 | **曲線=済 / 操作の流れ=済** |
| 8 | 円弧(3モード) | WP-04/08 | **曲線=済 / 操作の流れ=済**(3モードとも) |
| 9 | ベジェ | WP-04/08 | **曲線=済 / 操作の流れ=済** |
| 10 | スプライン | WP-04/08 | **曲線=済 / 操作の流れ=済** |
| 11 | 移動 | WP-04/08 | **幾何=済 / 基準点の受け取り=済** |
| 12 | コピー | WP-04/08 | **幾何=済 / 基準点の受け取り=済** |
| 13 | ミラー複製 | WP-04/08 | **幾何=済 / 基準点の受け取り=済** |
| 14 | 回転 | WP-04/08 | **幾何=済 / 基準点の受け取り=済** |
| 15 | 分割 | WP-04 | **幾何=済**(Split) |
| 16 | トリム | WP-04 | **幾何=済**(TrimCurve) |
| 17 | 延長 | WP-04 | **幾何=済**(ExtendCurve / ExtendCurveToBoundary) |
| 18 | 端点一致 | WP-04 | 未 |
| 19 | 接線接続(G1) | WP-04 | 未 |
| 20 | 曲率接続(G2) | WP-04 | 未 |
| 21 | 測定 | WP-08 | **幾何=済**(§5) / Qt=未 |
| 22 | 2点を線で結ぶ | WP-08 | **操作の流れ=済** |
| 23 | 面取り/丸めのペア選択 | WP-08 | **幾何=済**(ChamferLines / FilletLines) / Qt=未 |

V1は「このツールだけ Esc が効かない」「このツールだけ取り消しができない」があった。
V2はツールごとの分岐を持たず、全ツールが同じ状態機械の上に載る。
そのため取り消し・中断・途中プレビューは全ツールで同じように動く(試験で確認済み)。

## 2. 編集アクション

| V1 | V2での担当 | 状態 |
| --- | --- | --- |
| 一致解除 | WP-03(Feature削除) | 未 |
| 滑らか解除 | WP-03(Feature削除) | 未 |
| 結合(join) | WP-04 | **幾何=済**(AnalyzeChain) |
| 2線を交点まで | WP-04 | **幾何=済**(MeetLines) |
| C面取り(複数ペア一括、離れた線は交点まで自動延長) | WP-04 | **幾何=済**(ChamferLines。離れた線も可) |
| R面取り(同上) | WP-04 | **幾何=済**(FilletLines) |
| 角の加工(ポリラインの角を落とす/丸める) | WP-04 | 未 |
| 交点に点(2本の交点すべてに作図点) | WP-04 | **幾何=済**(IntersectCurves) |
| オフセット(平面ワイヤーの平行オフセット) | WP-04 | **幾何=済**(直線・円弧。自由曲線は断る) |
| 基準線に設定 / 基準解除 | WP-08 | 未 |
| 補助線として作図 / 補助線化 | WP-03/08 | 未 |

**注意: 「オフセット」「交点に点」「2線を交点まで」「角の加工」「基準線」は
`command-catalog.md` に対応するIDが無い。** 台帳へ追加すること。

## 3. 円弧の作り方(3モード)

| V1 `ArcDrawingMode` | V2 | 状態 |
| --- | --- | --- |
| ThreePoints | `ArcThroughThreePoints` | **済** |
| EndpointsRadius | `ArcFromEndpointsAndRadius` | **済** |
| StartTangent | `ArcFromStartTangentRadiusSweep` / `...Length` | **済** |

`PRD-052` はさらに「始点+接線/法線方向+半径/角度+中心角/円弧長」を要求しており、
V1の3モードより広い。**V1同等は満たしたうえで、PRD-052の残りも満たすこと。**

## 4. スナップ(8種)

| V1 `DrawingSnapKind` | V2 §6.1 の優先順位に有るか | 状態 |
| --- | --- | --- |
| Point(作図点) | 有 | **済**(`SnapKind::DrawingPoint`) |
| Intersection(幾何交点) | 有 | **済**(`SnapKind::Intersection`) |
| Endpoint(端点) | 有 | **済**(`SnapKind::Endpoint`) |
| Midpoint(中点) | 有 | **済**(`SnapKind::Midpoint`) |
| Center(円・円弧の中心) | 有 | **済**(`SnapKind::Center`) |
| **ProjectedPoint(作業平面へ法線投影)** | 無かった | **済**(`SnapKind::ProjectedOnPlane`) |
| **Extension(既存線分の延長線上)** | 無かった | **済**(`SnapKind::Extension`) |
| Grid | 有 | **済**(`SnapKind::GridMajor` / `GridMinor`) |

V2が新しく足したもの: 接点、垂足、四半点、曲線上最近点、画面交差(候補には出すが吸着しない)。

**ProjectedPoint と Extension は V2 の §6.1 から漏れていたので、`SnapKind` へ入れた。**
実装は `src/next/kachakacha/modeling/SnapEngine.*`。Qt に依存しないので、
画面の情報は `ScreenMapping` 1枚だけを受け取る。試験は `tests_v2/snap_tests.cpp`。

あわせて、V2が新しく足そうとしている「接点/垂足」「四半点」「曲線上最近点」は
V1に無い改善なので、そのまま入れてよい。

### 修飾キー

V1は **Ctrl** でスナップ抑止（操作ガイド `MainWindow.cpp:2442`「Ctrl中: 完全に吸着しない」）。
V2 `PRD-062` は **Shift**。既存利用者の再学習を避けるため、**両方効く**ようにする。
`SnapSettings::suppressed` は「どちらかが押されている」を表す1つの真偽値なので、
どちらのキーでも同じ挙動になる。トグルではなく、離せば直ちに戻る。

## 5. 測定(3モード)

| V1 `MeasurementMode` | V2 | 状態 |
| --- | --- | --- |
| TwoPoints(全長・dX・dY・dZ・各面への投影距離・各軸との角度) | `PRD-070` | **幾何=済**(`MeasureTwoPoints`) |
| ThreePointsAngle | `PRD-071` | **幾何=済**(`MeasureThreePointAngle`) |
| Elements(線同士・線と曲線・曲線同士の接線角と法線角) | `PRD-071` | **幾何=済**(`MeasureTangentAngle` / `MeasureNormalAngle`) |
| 線の長さ・半径・接線・曲率法線 | — | **幾何=済** |
| 点と線・線と線の最短距離 | — | **幾何=済**(`MeasurePointToCurve` / `MeasureCurveToCurve`) |
| 方向と平面の角度・平面どうしの角度・点と平面の符号つき距離 | — | **幾何=済** |
| 測定結果から作図点・中心点・制御点を作る | `PRD-072` | 未(WP-08) |
| **残した参照寸法(永続の注記)** | **V2に無い** | **仕様の欠落。復活させる** |

**オーナー指示 2026-09-08「v1にあった測定機能も必要だ」により、この節は完了条件である。**
V1の `Measurement.h` の公開関数はすべてV2へ移した(上の表)。
V2ではさらに PRD-070 の dX/dY/dZ・各座標面への投影距離・各軸との角度を足した。

**「残した参照寸法」は V1 の情報タブにあり、V2 §12 は「選択履歴を直近10件」しか
持っていない。永続の注記として復活させること。**

## 6. 数値入力

| V1 | V2 | 状態 |
| --- | --- | --- |
| 四則演算・括弧・単項・pi | `PRD-036` / geometry §11 | **済** |
| — | `^` `deg()` `rad()` 単位suffix 全角 式の保持 | **済**(V1より広い) |
| 作図開始後の寸法欄への自動フォーカス | `PRD-053` | 未 |
| マウス位置近くでの入力値確認 | `PRD-053` | 未 |

## 7. 作業平面

V1は「標準面 / 法線方向オフセット / 平面内X軸まわり角度」ほかを持つ。
V2 `PRD-055` と `AT-WPL-001` は11方式を要求しており、V1より広い。
**V1でできた作り方が11方式に含まれているかを、WP-08着手時に1つずつ突き合わせること。**

## 7.5 いまの進み具合(2026-09-08)

幾何の計算そのもの(WP-04)は、上の表で「幾何=済」と書いた行まで入っている。
**「済」は幾何のみで、画面から使えることはまだ意味しない。** UI接続はWP-08。

受入試験の内訳: 曲線20 / 数式15 / 鎖14 / 円弧12 / 編集28 / 測定16 = **105件**。

## 8. この表の使い方

1. WPを完了にする前に、そのWPが担当する行の状態を更新する。
2. **「未」が残っている行がある限り、WP-12(全受入)を完了にしてはならない。**
3. 落とす判断をした行は、`product-contract.md` §14 か §15 へ移し、この表から消す。
   移した記録をこの節へ1行残す。

### 落としたもの(オーナー確認済み)

- （まだ無し）
