PASS

# 検証した実測証拠

- `git diff --stat` 実測: 8 files, **1049 insertions(+), 12 deletions(-)**。ステージ済み・未追跡ファイルなし。V1 ファイルも無関係ファイルも含まれない。パケット本文の「1042 insertions」「Build/Test 未実行」は実測と食い違うので、そこは採らない。
- ビルド成果物 `build-msvc2022-x64/Release/kachakacha_cad_next.exe` は 09-13 01:08、`LastTest.log` は 01:11。変更 8 ファイルの最終更新は 00:45〜00:56 で、すべてビルド前。証拠は今回の差分に対応している。
- `LastTest.log`: 失敗行ゼロ。`selection_tests 38/0`、`cad_next self-test 162/0`、`architecture_tests 12/0`（`v2_core_has_no_qt_or_occt_include` / `file_line_limit` / `function_line_limit` / `no_unfinished_marker` を含む）。新規 11 件（`矩形の向きで取り方が決まる` ほか）と自己試験 2 件（`矩形選択は向きで包含と交差を分ける` / `Ctrl+矩形で足し引きし、小さな動きは矩形にしない`）の PASS を行単位で確認。

# Review checks の結果

1. **CURRENT_TASK の受け入れ試験**: 4 件すべて実装と試験に対応。左→右は `MakeBoxSelection` の `end.x >= start.x` → `Contained` と `BoxReach::Taken` の `fullyInside` 要求、右→左は `touched`（`BoxTouchesSegment` の Liang–Barsky で弦の横断も拾う）、Ctrl は `SelectionModeFor` → `ApplyBoxSelection` の Toggle、5px 門は `BoxSelectionIsMeaningful`。
2. **§4.2 適合**: 条文（左ドラッグ=矩形／左→右は完全包含／右→左は交差／Ctrl は追加または解除／Shift は選択集合に使わない）と一致。`SelectionModeFor` は Shift を無視して Replace のままなので Shift 条項にも反しない。矩形は `SelectionElementKind::Object`・segmentId なしで拾うので「対象を選ぶ」の語義に合う。
3. **無関係変更なし**: `SelectAt` の Ctrl 判定を `SelectionModeFor` へ寄せた 6 行だけが既存行への手入れで、挙動同値の重複排除。掃除目的の改造はない。`V2ViewportInput.cpp` / `V2ViewportDraw.cpp` は候補一覧外だが `V2Viewport` の実装分割先で範囲内。
4. **重複実装なし**: 数え方は core の `AccumulateBoxReach` / `BoxReach::Taken` 1 か所。`CollectBoxShapeCandidates` は網が画面側にあるため viewport に置いているが、規則は core を呼んでいる。`shapeViews_` は `guideShapes_`/`partShapes_`（EntityId 文字列キーの map）由来で 1 Entity 1 View のため、形側の物体単位集計が core の `BoxStateFor` と食い違う余地はない。`shapesVisible` と `SceneBuilder` の可視性フィルタもクリックと共有。
5. **距離は logical px**: `kBoxSelectMinimumDragPx = 5.0`、枠の線幅 1.4px。`GrabToMove` の 4px（`DragIsFarEnough` は閾値を引数に取れない固定値）と別定数にした理由もコメントにある。
6. **境界**: `src/next` に Qt/OCCT の混入なし（架構試験で確認）。`ScreenBox` / `BoxSelection` / `BoxReach` はすべて `geometry` 型のみ。
7. **状態分離**: `boxSelect_` は Hover / Selection / `cycle_` と別の構造体で持ち、引いている間は `mouseMoveEvent` で Hover・スナップを探さない。`Esc` は矩形だけを取り消して core の `EscapeAction` 段には触らない。`PruneSelection` が引きかけの矩形（＝「押した時点の選択」）を捨てるので、文書変更後に消えた物体を復元する穴がない。`ReleaseBoxSelect` は先頭で `boxSelect_` を空にしてから `SetSelection` を呼ぶので、選択変更通知が命令を走らせて `PruneSelection` へ再入しても二重取消にならない。
8. **退行の試験被覆**: 押下時の 1 件選択は維持し、離すときは `selectionAtPress` から当て直すのでクリックと矩形が混ざらない（自己試験の 3px ドラッグで確認）。右押しは矩形中に無視、`PickPending()` 中は構えない。`ApplyBoxSelection` の Replace 一括化・物体単位照合・重複潰しはそれぞれ独立に試験されている。
9. **誤魔化しなし**: TODO/stub/placeholder/暫定なし、許容差の緩和なし、無音のフォールバックなし。投影できない点は `fullyInside` を落とすので、カメラ裏へ伸びた対象が「完全包含」にならない。退化線分は `BoxTouchesSegment` の分母 0 経路で正しく外へ弾かれる。Liang–Barsky の inline 版は `enter <= leave` の不変量を各 clip で保つため、末尾の `u1 > u2` 判定を省いても正しい。
10. **記録の整合**: 上記のとおり実測で確認済み。

# 次に触る人への申し送り（差し戻し理由ではない）

1. `mousePressEvent` の中ボタンには、右ボタンと違って矩形中のガードが無い。左を押したまま中を押すと `panning_` は立つが `mouseMoveEvent` が `boxSelect_` を先に見るためパンが効かない（カーソルだけ手の形になる）。§5.2 に照らして、右ボタンと同じガードへ揃えるのが素直。
2. 矩形は Object 参照を作るため、矩形で選んだ線を Ctrl+クリックしても Edge 参照として別枠で足され、`SelectionItemCount` が 1 物体に対し 2 を返す。`SelectAllOfKind` や `entityIds` 由来の Entity 単位参照でも以前から起きる既存挙動で本差分の欠陥ではないが、矩形が主要な手つきになる分だけ露出が増える。
3. 命令を構えたまま（`pendingCommandId_` あり）物体の上で押すと、押下時の `SelectAt` が選択変更通知経由で命令を走らせ、矩形が始まる前に文書が変わって `PruneSelection` が矩形を捨てる。クリックでも同じことが起きる既存経路だが、矩形としては無反応に見える。
4. 塗った形は稜線でのみ判定するため、面の内側だけを囲む矩形では部品を選べない。作業平面は矩形でもクリックでも拾えない（新たな欠落ではない）。
5. `docs/v2/acceptance-coverage.md` の AT-UIX-004 に「矩形」が残件として残っている。同じ行の Tab/Alt も未更新なので片方だけ消さなかった判断は一貫しており、台帳の締めは AT-UIX-004 を閉じる担当の仕事。
6. `V2Viewport.cpp` は 1485 行で 1500 行門のすぐ手前。次にこのファイルへ足すときは分割案と一緒に。
