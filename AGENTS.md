# kachakachaCAD 作業ガードレール

このリポジトリは、模型製作のためのCADを作る場所です。作業者やAIエージェントは以下を守ってください。
この文書はエージェントの行動規範であり、製品の詳細仕様は `docs/` 側に置きます。

## このソフトが目指すもの(要約)

- **完成品のモデリングから実際の模型工作までを一続きにする模型製作CAD。** 主戦場は鉄道模型だが、
  ガンプラのような自由曲面のモデリングにも使う。専用CADに閉じない。
- 基本フロー: **完成品をモデリングする → 部材近似モデルへ変換する → 型紙(面展開図)を出力する →
  必要な治具はユーザー自身が同じソフトで設計する → プラ板・紙・3Dプリンタで作る**。
  治具の自動生成は補助機能にすぎない。治具はモデルと同格の設計対象。
- 作図は両輪: ①3D空間に紙(作業平面)を置いてその上で正確な2D作図、
  ②3D空間上でも任意の点・交点から任意の箇所へ直接線を引く。面取り(C/R、任意半径)も2D/3D両方。
- スケールに主対象は無い(テストケースはHO/Nでよい)。将来は全体n倍・参照寸法基準のスケーリングを行う。
- 最終的に他人へ配布する。
- 詳細は `docs/product-principles.md`、面展開の仕様は `docs/surface-unfolding-spec.md`。

## 変えてはいけない中核思想

- プラ板・紙・3Dプリントとして**実際に作れること**を常に優先する。工作できない見た目だけの形状を作らない。
- 自動分割で人間が作れない細切れ部品を勝手に大量生成しない。少数の大きな部材+手動介入が原則。
- 2D作図と3D空間を別ソフトのように分断しない。
- ワイヤーを作業平面の所有物にしない(ADR 0003)。
- 未決定の大きな仕様は、複数案を提示してから決める。

「三角形メッシュを主編集対象にしない」「押し出し+ブーリアン中心にしない」等の従来の禁止事項は
**「現段階では優先しない」**に読み替える(オーナー確認済み。将来はやる)。今それらに向けて設計を
歪めないこと、および今それらの実装を始めないことだけを守る。

## 読む文書

**常時(毎セッション):** この文書、`docs/product-principles.md`、`docs/refactoring-plan.md`(チェックリストと既知の問題)。

**触る領域に応じて:**

| 領域 | 文書 |
| --- | --- |
| 作業平面・作図・ワイヤ | `docs/workplane-spec.md`, `docs/wire-and-sketch-spec.md`, ADR 0002/0003 |
| 面・投影 | ADR 0004, `docs/surface-unfolding-spec.md` |
| 板・展開・型紙 | ADR 0005〜0017, `docs/surface-unfolding-spec.md` |
| UI(パネル・ビューポート) | ADR 0018, `docs/usability-review.md` |
| .kcd 保存形式 | `docs/project-script-format.md` |
| ビルド・環境 | `docs/setup-windows.md`, `docs/multi-machine-development.md`, `docs/linux-build.md` |
| 計画全体 | `docs/roadmap.md`, `docs/minimum-usable-completion-plan.md` |

## 役割分担と質問の基準

- オーナーは「仕様の大枠決定」と「完成した動作品のチェック」だけを行う。
  エージェントは実装 → push → ビルド → 起動確認まで自走する。
- オーナーに質問してよいのは: 中核思想に触れる変更、取り返しのつかない操作、
  複数案が拮抗する大きな仕様判断。それ以外は妥当な案を選び、判断はADRか該当文書に記録して進む。
- **目視確認はオーナーの受入作業であり、エージェントの完了条件ではない。**
  エージェントの完了条件は下の検証ゲートまで。

## 検証ゲート(環境別)

原則: **本番環境=Windows実機で検証する。** 必要な開発ツールはローカルPCへ導入してよい(オーナー指示済み)。

- **Windows(本番)**: `.\scripts\check.ps1`(構成+ビルド+テスト)と `kachakacha_cad.exe --self-test` を通す。
- **Linux/クラウド**: `./scripts/check.sh linux-core` が最低ライン(コアのみ)。
  Qt・OCCT・UIに触れる変更は、Windows実機かCIで通るまで「検証済み」と言わない。
- **main は常にグリーンに保つ。** main への push は CI(`.github/workflows/windows-build.yml`)を起動し、
  `ci-latest` タグの配布zipを直接更新する。**mainを壊すことは配布物を壊すこと。**
  Windowsで未検証の大きな変更はブランチに置き、CIが通ってからmainへ入れる。

## コード方針

- 幾何コアはUIから独立させる。Qt・OCCTへ依存するコードは境界層に閉じ込める。
- 数値誤差を前提に、比較には許容誤差を使う。近似の誤差は隠さず数値(最大/RMS)で報告する。
- 新機能には、最低限の自動テストか検証手順を添える。
- **文字コード:** ソースはUTF-8(BOM無し)。日本語コメント可(MSVCは `/utf-8` 指定済み)。
  CP932で保存し直さない。BOMを付けない。改行は `.gitattributes` に従う(基本LF、`.ps1`/`.cmd`/`.bat` のみCRLF)。
- **`src/apps/cad/MainWindow.cpp` と `MainWindowSelfTest.cpp` の `#include`・`using` 宣言は常に一致させる**
  (片方だけ更新するとWindowsビルドが壊れる。過去に2回発生)。
- **ライセンス:** 配布前提。依存は配布自由なライセンスのみ(現行 Qt6/OCCT はLGPL・動的リンク運用)。
  GPL・商用限定・ソース公開義務が配布を縛るライブラリを追加しない。依存を追加したら
  `docs/licensing-audit.md` に追記する。

## コード衛生ガードレール

数値は必ず計測してから使う(文書内の固定値は信じない):

```bash
wc -l src/apps/cad/MainWindow.cpp          # 2026-09-02時点 10,449行
grep -c "tool_ ==" src/apps/cad/CadViewport.cpp   # 同 128箇所
```

- `MainWindow.cpp` への新規追加は原則禁止。新しいUIパネル・機能は独立したクラス/ファイルにする
  (ADR 0018)。1つの `.cpp` が3,000行を超えている場合、その拡大は分割提案とセットでのみ許す。
- ほぼ同一の関数を3つ以上コピペしない。3回目でパラメータ化する。
- `ViewportTool` 追加時に `tool_ ==` 比較の散在を増やさない。着手前に上のコマンドで件数を測り、増加を最小化する。
- 名前参照フィールドを追加したら `docs/refactoring-plan.md` の名前参照一覧に追記する。
- `docs/refactoring-plan.md` のチェックリスト項目に着手するときは、**最初に状態を「進行中」へ書き換えて
  コミット・pushする**(複数エージェントの同時着手を防ぐロック代わり)。完了時も同様に更新する。

## 複数PC開発のガードレール

詳細な手順(クラウドからの bundle 受け渡しを含む)は `docs/multi-machine-development.md`。

- 作業開始前に必ず同期する(Windows: `scripts\sync.ps1` / Linux: `./scripts/sync.sh`)。
- **作業ツリーが dirty のまま新しい作業を始めない。** 誰かの未コミット変更を見つけたら、
  まず `wip-<PC名>-<日付>` ブランチへ退避コミットして push してから作業する。
- そのPC・環境での作業を終えたら必ずコミットして push する。push できない環境(クラウド)は
  bundle 受け渡し手順で必ずGitHubまで届ける。未pushのまま放置しない。
- 特定PCの絶対パスをリポジトリへ書かない。依存の場所は `cmake/KachakachaEnvironment.cmake` の
  自動検出に任せ、足りなければ環境変数か `local.cmake`(git管理外)で渡す。
- ビルドは `CMakePresets.json` のプリセットを使う。Windows専用手順を足したら `scripts/*.sh` にも対を作る。

## 迷走防止チェック

実装前に確認する。

- その作業は模型製作の工程を短くするか。
- 完成品モデリング/部材近似/型紙/治具/出力のどの段階に効くか。
- ユーザーが直接編集する対象は何か。誤って他の段階の物を壊さないか(セットの「参照のみ」を使えるか)。
- 出力・実製作にどうつながるか。実際に作れる形か。
