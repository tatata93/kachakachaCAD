# 近似後の制作ワークフロー提案(2026-09-06)

指摘「近似した後の制作ワークフローがまとまっておらず迷走している。実際の物理空間を
考慮してワークフローを詰めて機能を決めたい。提案しろ」への回答。

---

## 1. 指摘の認識

1. **曲げ確認が部材単位でない**: 部材1を選んでスライダーを動かすと全部材が動く。
   選んだ部材(複数可)だけを変形させたい。※現状、選択は「表示の絞り込み」にしか
   効いておらず、変形は常に全部材一括。
2. **板材化も選択部材のみに**: 実は出力範囲自体は選択部材に絞られているが、
   姿勢が「0%か100%かの二択」で、しかもプレビューと出力で姿勢計算が別実装のため、
   見えている形と出てくる形が一致しない。範囲・姿勢・位置を全部そろえて直す。
3. **「この曲げ状態を板材化」→「この曲げ状態を出力」**: 出力内容(ワイヤ/面/板材/
   可変を維持するか)と出力先(新kcd/同一kcd内の新グループ/STL/STEP)を選択式に。
4. **可変面・可変ワイヤの派生も曲げに追従**: これらから作った板材・オフセット面・
   オフセットワイヤが姿勢変更で置き去りになる。追従させる。
5. **複数モデル出力時の位置関係**: 曲げ確認で見えている位置関係のまま出力する。
6. **根本**: 近似後のワークフローが定義されていない。

## 2. 迷走の原因(自己分析)

機能をボタン単位で足してきた結果、**「姿勢」「派生(追従)」「複製(固定)」の3概念が
整理されないまま**、板材化・STL・別kcd・厚み化・オフセット・投影複製がそれぞれ
別々の振る舞いを持っている。プレビュー(曲げ確認)と実体化の幾何計算も別実装。
概念を3つに絞り、全機能をそこへ載せ直すのが本提案の骨子。

## 3. 実際の物理空間での制作の流れ

プラ板・紙の模型製作を分解するとこうなる:

1. **形を決める** — 完成品の姿。
2. **部材に割る** — 素材は基本1軸にしか曲がらないので、曲面を「1軸曲げの帯」の
   集合に分ける。ここで「この割り方で本当に作れるか」が決まる。
3. **一部材ずつ確かめる** — 型紙の形・曲げの向き・曲げ角。実作業でも
   「この1枚をどう曲げるか」を部材ごとに考える。全体が一斉に曲がる確認は実態と合わない。
   **→ 指摘1はこの段階の話**。
4. **組立の支えを作る** — 曲げた帯は自立しない。胴枠(輪切りの骨)や治具に当てて
   位置を決めて貼る。ビジョン合意どおり治具・胴枠は**ユーザー自身が同じソフトで
   設計する対象**(自動生成は補助)。設計するには「近似後の実形状の線」が要る。
5. **出力する** — 型紙(1:1、番号+山谷+曲げ角)、確認用3D(STL/STEP)、
   治具・胴枠(3Dプリントや板から切り出し)。

## 4. CADワークフロー(提案): 5段階と画面対応

| 段階 | やること | 画面 | 状態 |
| --- | --- | --- | --- |
| A 形 | 完成品の面・板材を作る | 作図/面・板材 | あり |
| B 割る | 近似モデル作成、境界・開口・接続スコープ調整 | 近似モデル/部材近似を作成 | あり |
| C 確かめる | **部材単位**の姿勢(曲げ角・組立進行)、型紙確認 | 近似モデル/姿勢(旧: 曲げ確認) | 改修(指摘1) |
| D 支える | 胴枠・治具を、近似に**追従する派生線**を土台に設計 | 作図(通常ツール)+派生線出力 | 一部(接続スコープ)。断面線が未 |
| E 出す | 「この曲げ状態を出力」(内容・形式・出力先を選択) | 近似モデル/姿勢と出力 | 改修(指摘2,3,5) |

## 5. 3つの基本概念(全機能をこれで説明する)

### 姿勢(ポーズ)
- 近似モデルが持つ、保存される状態: **折り線ごとの角度** + **部材ごとの組立進行度**。
- スライダー・折り角行は**選択中の部材だけ**に効く(未選択=全部材)。
- 各部材は「展開して脇に並べた位置」↔「近似モデル上の完成位置」を**独立した剛体**として
  補間する。部材同士を連鎖させない(隣が動いたから釣られて動く、をしない)。
  → 部材1だけ0%、部材2は100%、が普通にできる。
- プレビュー(ドラッグ中)も確定も派生の再生成も出力も、**同じ1つの姿勢計算関数**を使う。
  「見えている形=出てくる形」を実装レベルで保証する。→ 指摘5は自動的に解決。

### 派生(可変)
- 近似モデルに名前でぶら下がり、**元モデルの編集にも姿勢の変更にも自動追従**して
  再生成されるオブジェクト。
- いま派生なのは: 部材面・縁ワイヤ・穴、接続スコープ、部材面上の板材。
- **ここに統一して追加するもの(指摘4)**: 部材面のオフセット面、縁・穴からの
  オフセットワイヤ、厚み化の独立出力([ワイヤ][面])、投影輪郭の厚み位置複製。
  「可変な物から作った物は可変」を原則にする。

### 複製(固定)
- いまの姿勢の形をただのオブジェクトとして写したもの。以後は追従しない。
- STL/STEP は物理的に必ず固定。kcd内へも「固定で出す」を選べる(改造のベースに
  したい時など)。

## 6. 「この曲げ状態を出力」ダイアログ仕様(指摘2・3・5)

「この曲げ状態を板材化」「STL保存」「STEP保存」「別の.kcdへ保存」の4ボタンを
**1ボタン+ダイアログ**に置き換える:

- **範囲**: 選択中の部材のみ(未選択なら全部材)。一覧の選択がそのまま範囲。
- **内容**: ☑縁ワイヤ ☑面 ☑板材(厚み __ mm)。開口(穴)は面・板材に自動で付く。
- **形式**: ○可変(近似モデルに追従する派生として出す) ○固定(いまの形の複製)。
- **出力先**: ○このkcdの新グループ(既定) ○別の.kcd ○STL ○STEP。
  STL/STEP を選ぶと形式は固定に自動確定。可変は同一kcd内のみ有効。
- **位置**: 3Dビューの曲げ確認で見えている位置そのまま。複数モデルを順に出力しても
  位置関係は画面どおり(姿勢計算の一本化で保証)。

## 7. 段階D(組立の支え)への提案: 断面線の派生出力

胴枠・治具設計の土台として、**近似モデルの輪切り断面線を派生ワイヤとして出せる**
ようにする(位置を指定して「断面線を作る」→ `<モデル>_断面@z=40` のような派生ワイヤ)。
境界編集や姿勢の変更(完成形想定なら100%)に追従するので、胴枠の輪郭を
この線から作図すれば、近似をいじっても骨が実形状からズレない。
合意12(治具は近似の線へ接続)の具体化。のりしろは合意5どおり作らない。
※これだけ新機能なので、やるかどうかはオーナー判断。

## 8. 実装計画(提案順)

1. **第1弾 姿勢の部材単位化**(指摘1):
   部材ごとの組立進行度(kcd保存)、スライダー・折り角行の選択スコープ化、
   剛体独立補間。コア+UI+テスト。
2. **第2弾 姿勢計算の一本化**(指摘5の土台):
   プレビュー・派生再生成・実体化が同じ関数を通るよう統合。
3. **第3弾 出力ダイアログ**(指摘2・3): 上記6の仕様。既存4ボタン置き換え。
4. **第4弾 派生追従の拡張**(指摘4): オフセット面・オフセットワイヤ・
   厚み化独立出力・厚み位置複製を姿勢追従の派生に統一。
5. **第5弾 断面線の派生出力**(上記7。オーナー判断待ち)。

各弾ごとに PCビルド 17/17+スクショ確認まで回してから次へ進む。

## 9. 決めどころ

- この5概念・5段階の方針で進めてよいか。
- 第5弾(胴枠用の断面線)をやるか。
- タブ名「曲げ確認と出力」→「姿勢と出力」への改名可否(些細。指定なければ改名する)。
