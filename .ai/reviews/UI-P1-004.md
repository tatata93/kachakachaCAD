PASS

Codex CLIの利用上限により、独立したClaudeセッションへ最終レビューを切り替えた。

- Windows configure/buildは成功した。
- CTestは128/128件成功した。
- アプリ自己試験は160/160件成功した。
- 右クリック候補は既存コマンドより前に表示される。
- 選んだ候補は部分要素参照を保持したままReplace選択される。
- 空白と単一候補の右クリックは既存選択を消さない。
- 右ドラッグはカメラを動かさずメニューも開かない。
- 中ドラッグとShift+中ドラッグは退行していない。

非阻害事項: 実装担当の報告に「グループ名も表示」とあるが、実コードは作業票どおり物体表示名と部分要素種別を表示する。判定には影響しない。
