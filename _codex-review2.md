# kachakachaCAD 画面の分かりやすさレビュー

## 結論

現状は「機能がない」のではなく、**同じ目的への入口と説明が多すぎ、しかも入口ごとに呼び名と手順が少しずつ違う**ことが最大の問題です。個々のツールチップはかなり丁寧ですが、初めての利用者はツールチップを総当たりしません。上部のモード→道具→右パネル→操作ガイドが一本の手順として読めるように揃える必要があります。

レビューは指定された6領域のソースを静的に読み、表示文言、初期表示、セクション切替、失敗時メッセージを追ったものです。実画面の描画品質やウィンドウ幅ごとの崩れは評価対象外です。

## 1. 初めて使う人がつまずく所（重大順）

### 1. 操作ガイドが、選んだ出力道具・近似モデル道具に追従しない

- **どこが問題か:** `MainWindow.cpp` の `RefreshBeginnerGuide`（2321行から。`setGuide` は2383行付近）の `case 3` は常に「出したい物を選んで『選択を追加』」、`case 6` は常に単一面近似寄りの「近似元と分割条件を指定」です。一方、上部には出力3道具（`MainWindow.cpp` 1515–1522行）と近似モデル4道具（1536–1543行）があり、`ShowOutputTool` / `ShowPartModelTool` で右パネルだけが切り替わります。
- **なぜ迷うか:** 「1:1図面」や「ペーパークラフト展開」を選んでも、左下は出力表と3Dプレビューへ誘導します。「一覧・型紙」「曲げ確認と出力」「セット」を選んでも、ガイドは作成手順のままです。画面内の二つの指示が衝突します。
- **具体的な直し方:** `ShowOutputTool` と `ShowPartModelTool` からガイド更新を必ず呼び、選択道具ごとに文面を分けます。例: 1:1図面は「次: 出力する作業平面を選ぶ」→「対象（全線/選択線）を決める」→「SVGまたはDXFを保存」。ペーパークラフトは「次: 3D画面で板材を1枚選ぶ」。近似の「一覧・型紙」は「次: 一覧でモデルまたは部材を選ぶ」、「セット」は「次: セットを選び、表示状態を決める」。
- **優先度:** 高

### 2. 「押し出し・厚み化」の中に、実質二つの厚み化手順が並ぶ

- **どこが問題か:** 道具名は `MainWindow.cpp` 1451–1453行の「押し出し・厚み化」ですが、右パネル `BuildSurfacePanel` では「押し出し」の中に「板材にする（厚みを付ける）」チェック（`MainWindowPanels.cpp` 1581–1599行）があり、その直後に別の「厚み化（ワイヤ・面・板）」一式（1621–1703行）があります。ガイドも「面を選べば厚み化と同じ」「詳しくは同じ画面の下」と説明します（`MainWindow.cpp` 2586–2593行）。
- **なぜ迷うか:** 面を板にしたい初心者は、上のチェックと下のフォームのどちらを使うべきか判断できません。「同じ結果」と言いながら設定項目と実行ボタンが別なのも不安を生みます。
- **具体的な直し方:** 右パネル先頭で目的を二択にします。`○ 線を奥行方向へ伸ばす` / `○ 面に板厚を付ける`。選択に応じて一方のフォームだけ表示し、ボタンも「線を押し出す」「面を板材にする」に変えます。可変厚み等は「詳細設定」を開いたときだけ表示します。「押し出しで板材にする」チェックは面選択時には出さず、線から側面板を作る場合だけに限定します。
- **優先度:** 高

### 3. 近似モデルの主手順が、表の専門語を理解しないと開始できない

- **どこが問題か:** `PartModelPanel` の主入口は「選択をユニットの表へ取り込む」→4列の表「対象/種類/役割/部品」→「ユニットを近似」です（`PartModelPanel.cpp` 79–154行）。重要な意味である「近似する」「形状維持(接続)」「対象外」と部品番号の説明は表のツールチップにしかありません（114–118行）。
- **なぜ迷うか:** 模型製作者にとって「ユニット」「形状維持(接続)」「部品番号」は、車体のどの面をどう選ぶかに変換できない語です。特に「対象外」を表へ入れる理由と、「形状維持」が何に接続されるのかが見えません。
- **具体的な直し方:** 表の上に常時表示の3ステップを置きます。「1 車体の面・板をまとめて選ぶ」「2 各行を『板に分ける／形を変えず一緒に動かす／使わない』にする」「3 『近似を作る』」。役割セルの選択肢もこの平易な文言にし、専門名は補足へ退避します。部品列は「同じ番号＝同じ部品」の常時説明と、車体正面・側面の小さな具体例を添えます。「ユニット名」は「まとまりの名前（例: 前頭部）」へ変更します。
- **優先度:** 高

### 4. 近似モデル作成に「主方式」と「従来方式」が同じ画面で競合する

- **どこが問題か:** `PartModelPanel.cpp` 164行以降に「―― 1面だけ手早く近似（従来方式） ――」が主方式の直下に常設され、こちらにも「部材近似モデルを作成」ボタンがあります（224行）。
- **なぜ迷うか:** 初見の人には、どちらが推奨で、結果に何の差があり、車体の前面だけならどちらを使うべきか分かりません。「従来方式」は開発履歴の語であり、利用者の判断材料ではありません。
- **具体的な直し方:** 従来方式は「1枚の面だけを分割する（詳細）」という折りたたみへ入れ、初期状態は閉じます。主ボタンは「選んだ車体部分から部材を作る」、詳細側は「この1面を帯状に分ける」と結果が読める名前にします。両方式の差を2行で常時表示します。
- **優先度:** 高

### 5. 出力表の適用範囲が「すべての出力」に見えるが、1:1図面と展開には効かない

- **どこが問題か:** `BuildOutputPanel` の説明は「表の中身がそのまま .kcd / STL / STEP の出力対象」（`MainWindowOutput.cpp` 810–812行）で、これは正確です。しかし上部モード名は包括的な「出力」、道具名は「出力するもの」であり、隣に「1:1図面」「ペーパークラフト展開」があります（`MainWindow.cpp` 1515–1522行）。操作ガイドも「表の中身がそのまま出る」と強調します（2651–2654行）。
- **なぜ迷うか:** 利用者は表へ入れた板材が展開対象にもなると自然に解釈します。しかし展開は3D選択した板材1枚、平面図はコンボで選んだ作業平面が対象です。
- **具体的な直し方:** 最初の道具名を「3Dモデル・プロジェクト」に変え、見出しを「STL / STEP / .kcd に含めるもの」にします。操作ガイドにも「※この表は1:1図面と展開には使いません」を常時表示。出力モード上部には `3Dモデル` / `平面の線図` / `板材の展開図` の目的別3択を置きます。
- **優先度:** 高

### 6. 「3Dモデルを出力する…」は、出力ではなくプレビューを開くボタン

- **どこが問題か:** `MainWindowOutput.cpp` 874–880行のボタン「3Dモデルを出力する…」は `ShowOutputPreviewWindow` を呼び、実際の保存ボタンはその下の「STLで保存」「STEPで保存」「.kcdで保存」です。
- **なぜ迷うか:** 省略記号付きの「出力する」は保存ダイアログを予告します。押すと別ウィンドウが開くだけなので、失敗または二重手順に見えます。逆に下の保存ボタンを押せば、橙色の自動ふさぎを確認せず保存できてしまいます。
- **具体的な直し方:** ボタンを「出力前の3Dプレビューを開く」に改名します。安全確認を意図するなら、保存3ボタンはプレビュー窓内に主配置し、元パネル側は「プレビューせず保存（非推奨）」として詳細へ退避します。橙色の凡例「橙色＝自動でふさいだ面」をボタン脇に常時表示します。
- **優先度:** 高

### 7. 面・板材モードの12道具が、工程ではなく実装機能の横並びになっている

- **どこが問題か:** `MainWindow.cpp` 1442–1471行で「面を作成」から「成形治具」まで12個をテキストだけのツールバーへ並べています。区切り線はありますが、「形を作る」「加工する」「工作用補助」の見出しはありません。
- **なぜ迷うか:** 「開口」「切れ目」「分割線」「板を分割」は似て見え、どれが3D形状を変え、どれが展開だけを変えるか名称だけでは判別できません。狭い画面では後半がオーバーフローに入り、存在自体が見えません。
- **具体的な直し方:** 右パネル上端に見出し付きの3群を置きます。`形を作る: 面 / 押し出し・板厚 / 回転`、`板を加工: 窓・穴 / 展開の切れ目 / 展開片を分ける / 板そのものを2枚に分ける / 貼り重ね`、`工作補助: ライトケース / 成形型`。ツール名も結果が読める動詞句にします。ゴードン面は「高度な曲面」の中へ入れます。
- **優先度:** 高

### 8. ペーパークラフト展開は、初期画面から設定量が多すぎる

- **どこが問題か:** `BuildOutputPanel` の展開フォームには、組み立て方、切れ目方向、再現度、V字、曲線追従、最小曲がり、組立プレビュー、折り線間隔、3D切り幅などが一列に並びます（`MainWindowOutput.cpp` 487–640行）。一部は無効化されても表示されたままです。
- **なぜ迷うか:** 初回に必要なのは「板材を1枚選ぶ→1:1 PDF」のはずですが、生成アルゴリズムの調整と3D組立出力が先に同じ重みで見えます。「立体再現度 5/10」も、精度・部品数・工作難度の何が変わるかラベルだけでは読めません。
- **具体的な直し方:** 初期表示は「選択板材」「作り方（1枚優先/分割接着）」「工作しやすさ ↔ 形状忠実度」「用紙」「1:1 PDFを保存」だけにします。他は「切れ目・折り目の詳細設定」と「組立途中の3D確認」に分けて閉じます。再現度ラベルは「工作しやすい 1 — 形を忠実に再現 10」とし、現在値の下に予想分割数・最大誤差を表示します。
- **優先度:** 高

### 9. 「面を作成」の通常・おまかせ・ゴードンが重複して見える

- **どこが問題か:** `BuildSurfacePanel` には作り方コンボ「自動（選択から判定・推奨）」と「選択ワイヤーから面を作成」があり、その直下に別の主ボタン「おまかせで面を作る（選択順・向き不問）」があります（`MainWindowPanels.cpp` 987–999、1131–1147行）。さらに上部道具として「ゴードン面」が独立します。
- **なぜ迷うか:** 「自動」と「おまかせ」の違いが名前から分かりません。どちらも主ボタンなので、失敗したときにもう一方を試すべきか、別結果になるのか判断できません。
- **具体的な直し方:** 初期入口を一つの「選んだ線から面を作る」に統合し、自動連結・反転を既定にします。厳密な選択順を使う処理は「詳細: 線の順番と役割を指定」に移します。ゴードン面は「高度な曲面（縦横の線から作る）」と表示し、括弧で専門名を添えます。
- **優先度:** 中

### 10. 状態表示と実行ボタンで、選択方法が統一されていない

- **どこが問題か:** 例として投影ではコンボで「投影先の面」「平面図」を選びつつ、ワイヤーは3D選択です（`MainWindowPanels.cpp` 1183–1197行）。開口は「板材0枚 / 閉じた投影輪郭0本」と3D選択数を示します（1311–1328行）。近似は3D選択後に「表へ取り込む」、出力も同様です。
- **なぜ迷うか:** 画面ごとに「コンボで指定」「選択したまま実行」「一度表へ取り込む」が切り替わりますが、その方式の違いをレイアウトが示していません。選択が一時的なのか、登録済みなのかも判別しづらいです。
- **具体的な直し方:** 各道具の最上部を共通の「入力」枠にし、`選択中` と `登録済み` を明確に分けます。3D選択を使う欄には「3D画面で選ぶ」ボタンまたは選択チップ、固定コンボには「一覧から指定」を表示。実行ボタン直上に「入力OK: 板材1枚、輪郭1本」の検証行を置き、不足時はボタンを無効化して不足項目を赤字表示します。
- **優先度:** 中

### 11. 中核エラーは日本語でも、初心者向けの復旧手順になっていない

- **どこが問題か:** `CoreMessageJa.cpp` は広範囲を日本語化していますが、「ロフト」「ルールド面」「ガイド付きロフト」「許容値」「G1/G2」など内部の幾何用語が残ります（16–50、124、165、205–213行付近）。また `MainWindowSurface.cpp` 1667行の `planar work plane is unavailable` のような英語例外も残っています。
- **なぜ迷うか:** エラー理由が日本語でも、何を選び直すかが書かれていなければ初心者は復旧できません。未登録の英語文は日本語UIへの信頼を大きく損ないます。
- **具体的な直し方:** 対訳を「原因。次にすること」の二文に統一します。例: 「ルールド面には断面が2つ必要です」→「断面の線が2本必要です。3D画面で手前と奥の線をCtrl+クリックしてください。」、「交点の許容値」→「線同士のすき間として許す値」。UIへ届く例外の未翻訳テストを追加し、`planar work plane is unavailable` を「この輪郭の作業平面が見つかりません。元の平面図を選び直してください」にします。
- **優先度:** 中

### 12. 「Windows 95 風」が設定項目として強く見える一方、初心者支援とは無関係

- **どこが問題か:** `MainWindow.cpp` 1000行付近および1258行の設定「Windows 95 風の見た目」は、通常テーマと同じ設定メニューに露出します。
- **なぜ迷うか:** 初心者が理解すべき表示設定（線色、太さ、完成形表示）と趣味的テーマが同列だと、設定メニューの情報設計がぼやけます。またレビュー・マニュアルのスクリーンショットと見た目が変わり、案内との照合が難しくなります。
- **具体的な直し方:** 機能を消す必要はありません。「設定 > 外観 > レトロテーマ（Windows 95風）」へ隔離し、通常版を明示的に「標準」にします。操作ガイドや初回導線には出さないでください。
- **優先度:** 低

## 2. 用語の一貫性

| 現在の表現（場所） | 問題 | 置き換え案 |
|---|---|---|
| ワイヤー / ワイヤ / 線（`MainWindow.cpp` 1413、`MainWindowPanels.cpp` 1564、1654、`PartModelPanel.cpp` 300） | 同じ編集対象を3語で呼ぶ | 初心者向け表示は原則「線」。種類を示す必要がある箇所だけ「ワイヤー（つながった線）」 |
| 面・板 / 面・板材（タブ `MainWindow.cpp` 673、モード 1308） | モードと右パネルで名称が違う | すべて「面・板材」 |
| 平面 / 作業平面 / 作図面 / 紙（`MainWindow.cpp` 624、1344、2562） | 同じ対象か、役割の違う対象か判断できない | オブジェクト名は「作業平面」。現在描く対象だけ「現在の作業平面」。比喩の「紙」は説明文だけ |
| 近似モデル / 部材近似モデル / 近似ユニット（`MainWindow.cpp` 1309、`PartModelPanel.cpp` 79、84） | 概念の包含関係が画面から分からない | モード「工作用の部材」、生成物「部材モデル」、複数面のまとまり「部材グループ」 |
| 出力 / 保存 / 書き出し（出力パネル全般） | プロジェクト保存と製作用ファイル生成が混ざる | 通常プロジェクトは「保存」、外部形式は「書き出す」。ボタンは「STLを書き出す」等に統一 |
| 出力するもの / 出力対象 / 出力セット（`MainWindowOutput.cpp` の関数・表示） | 表の役割が曖昧 | 「STL / STEP / .kcd に含めるもの」または短く「3D出力リスト」 |
| 開口 / 穴 / 窓（`MainWindow.cpp` 1457、`PartModelPanel.cpp` 268） | 同じ機能が場所により変わる | 見出し「窓・穴（開口）」、以後は「窓・穴」。内部・詳細説明だけ「開口」 |
| 切れ目 / 切れ込み / 分割線（`MainWindowPanels.cpp`、`MainWindowOutput.cpp`） | 形状への影響が区別しにくい | 「展開を開く切れ目」「V字の逃げ切り」「展開片を分ける線」 |
| 板を分割 / 板材を分割 / 展開片の分割線（`MainWindow.cpp` 1461–1464） | 3D板を二つにする操作と型紙だけを分ける操作が酷似 | 「3Dの板を2枚に分ける」 / 「型紙をここで分ける」 |
| 積層 / 貼り重ね / 重ね板（UIと `CoreMessageJa.cpp` 226行付近） | 3語が混在 | 主表示は「板を貼り重ねる」。括弧で「積層」 |
| ゴードン面 / Gordon面 / 縦横の断面ネットワーク（`MainWindow.cpp` 1444–1446） | 専門名だけでは用途不明 | 「縦横の線から曲面を作る（ゴードン面）」 |
| ロフト / ルールド面（`MainWindowPanels.cpp` 991、エラー対訳） | CAD専門語 | 「3本以上の断面を通る曲面（ロフト）」 / 「2本の断面をつなぐ曲面」 |
| 形状維持(接続) / 接続範囲 / 〜_接続（`PartModelPanel.cpp` 114–149、`CoreMessageJa.cpp`） | 実装概念がそのまま出ている | 「形を変えず、近似部品につなぐ」 |
| 参照のみ（`PartModelPanel.cpp` 438–444） | 実際は「v1では表示のみ反映」とツールチップにあり、名前と挙動が不一致 | 実装が揃うまでは「薄く表示（編集防止は未対応）」、完成後に「参照表示（選択不可）」 |
| 立体再現度（`MainWindowOutput.cpp` 626） | 高いほど何が犠牲になるか不明 | 「形の忠実さ（高いほど部品・切れ目が増える）」 |

## 3. 機能はあるのに画面から辿り着きにくいもの

完全に到達不能と断定できる主要機能は多くありません。上部道具から各セクションへは接続されています。ただし、次は実質的に発見困難です。

1. **旧3D出力スコープUI** — `BuildOutputPanel` は `modelExportScope_` とSTL/STEPボタンを生成しますが、`modelSection->setVisible(false)` で隠し、`outputSections_` にも登録していません（`MainWindowOutput.cpp` 786–806、921–935行）。出力表へ統合済みなので旧UIを復活させる必要はありませんが、死んだUIを生成し続けると仕様理解を誤らせます。互換用データだけに分離し、画面部品は削除すべきです。
2. **部材グループを除外した .kcd 保存の前提設定** — 出力パネルには説明がありますが、除外設定自体は「モデルツリーの部材グループを右クリック」です（`MainWindowOutput.cpp` 893–905行）。右クリック専用操作は発見されにくいので、「除外するグループを選ぶ…」ボタンから同じ設定へ到達できるようにします。
3. **表示・線の詳細設定** — 右タブの見出しは隠されています（`MainWindow.cpp` 678–680行）。設定メニューの「表示・線の設定（表示タブ）」からは開けます（1249–1251行）が、「表示タブ」という実際には見えない名称が残っています。「表示・線の設定…」として独立した設定画面に見せるべきです。
4. **選択情報パネル** — `BuildInfoPanel` で作られる「選択情報」は隠れたタブ内にあり、通常は測定操作から `ShowRightPanel(5)` へ入るだけです（`MainWindow.cpp` 1982行）。測定を始めない人には辿れません。常設の選択要約をクリックすると開くようにします。
5. **投影輪郭を板厚位置へ複製** — 「押し出し・厚み化」を開き、長い押し出しフォームと厚み化フォームを下へ越えた先にあります（`MainWindowPanels.cpp` 1705–1725行）。治具・罫書きに有用ですが、独立した入口がありません。「板材を加工」群に「表裏へ輪郭を写す」として入口を置きます。
6. **近似モデルへの後付け穴** — 「一覧・型紙」内の長いボタン「選択ワイヤを部材へ投影して穴に」だけが入口です（`PartModelPanel.cpp` 266–284行）。面・板材モードの「窓・穴」から近似部材を選んだ場合にも、この機能へ案内すべきです。

## 4. 今のままで良いので触るべきでない点

1. **選択中の道具に必要な右パネルだけを表示する方針。** `RevealSurfaceGroup` と `ShowOutputTool`（`MainWindow.cpp` 1674–1717行）は、巨大な機能群を同時表示しないための正しい骨格です。問題は切替そのものではなく、道具名とガイドが揃っていないことです。
2. **失敗理由を近似パネル内へ残す設計。** `PartModelPanel.cpp` 156–162行の常設結果ラベルは、短時間で消えるステータスバーより明らかに良いです。他の複雑な作成機能にも横展開すべきで、ここをステータスバーだけに戻してはいけません。
3. **出力前プレビューで自動補修箇所を橙色にする考え方。** `MainWindowOutput.cpp` 858–878行の「開いている所を自動でふさぐ」と橙色表示は、実際に作れる閉じた形状かを利用者に見せる良い安全策です。ボタン名と凡例は直すべきですが、確認工程自体は維持すべきです。
