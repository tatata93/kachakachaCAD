# UI-P1-007 worker report

# Resumed existing repair

The task worktree already contains a repair made after a failed gate. The worker step was intentionally skipped; gates and both reviews must validate the complete current diff.

# Claude self-review

PASS

UI-P1-007 Stage 1/2 (foundation) passes, reviewed at EXTRA_HIGH depth; no corrections are needed before the next run. This pass edited nothing. The diff under review is the full working tree: 30 files, +1619/−198. It also contains Stage 2 work (connecting to the session and the viewport) and the repair made after the failed gate.

**Against the task and master specs**
- **Separate radii:** `GeometryTolerance` now has three separate radii: `displayPickPx=8` for points, `edgePickPx=6` for lines and `snapPickPx=12` for snapping.
  - Point picking uses only `displayPickPx`, line picking only `edgePickPx`, and snap collection and the screen-space intersection search only `snapPickPx`.
  - The old 6px/8px snap tiers are gone (`highPriorityRadiusPx`, `lowPriorityRadiusPx`, `IsHighPrioritySnap`).
  - Tests change one radius and check the other two don't move (`selection_tests`, `snap_tests`).
- **Same radius at any zoom:** distances are measured in screen px against the projected curve (`geometry::ApproachToCurveOnScreen`), not against the sampling chords. The perspective correction `s = t·w0/((1−t)·w1 + t·w0)` is derived correctly.
  - Tests zoom in on a circle and a Bezier curve, check snapping at 1 and 10 px/mm, and run the self-test at 200 mm and 20 mm view widths.
- **Snap hysteresis (holding the last target):**
  - A higher-ranked candidate takes over immediately.
  - At the same rank, the snap only switches when the new candidate is more than 4px closer.
  - The held target stays until 12 + 4px.
  - Pressing S, switching tools, cancelling and changing tool settings all drop it.
  - `PeekHover` in `V2MainWindow::RefreshGuide` no longer changes the held target from the screen centre.
  - Separate curves that overlap at the same point stay separate candidates, so the held target never jumps to the other curve.
- **§6.1 order:** `SnapPriorityRank` follows the six levels, with ties going to the closer candidate.
- **§6.2:** S suppresses snapping only while held. The Qt auto-repeat release does not end it, and losing focus clears it. The self-test sends real key events.
- **File format:** the two new keys save and load symmetrically. A missing key falls back to the default; 0, a negative value or a non-number is rejected with `KCD2-D002`. Both are tested, and `kcd2-format.md` §19.3 matches. The only change in the two sample files is the two added tolerance keys.

**Build and tests** (the latest gates ran at 20:35–20:40, after the last source edit at 20:33:17)
- Configure passed. The build passed with no warnings in changed files.
- CTest passed 129/129.
- The app self-test passed 169/169, including the new snap radius, hold and S-key case.
- The earlier Stage 2 failure was `v2_architecture_tests` (`V2Viewport.cpp=1509` lines, over the 1500 limit). Moving `focusOutEvent` into `V2ViewportInput.cpp` fixed it without weakening the test.

**TODO, stubs and unrelated changes**
- No TODO, stub or placeholder markers were found, `git diff --check` is clean, and there are no untracked files.
- V1 is untouched, and no Qt or OCCT include was added under `src/next`.
- Every file outside the task's file list follows from the work:
  - saving the new tolerances and regenerating the samples
  - moving the screen-to-curve distance out of `Selection.cpp` so picking and snapping share it
  - updating the manual and design docs from Shift to S
  - adjusting the self-test to click the placed point, and fixing a comment that still said lines are hit at 8px

**Not blocking (record for Stage 2 or the owner)**
1. `V2Viewport.cpp` is now exactly 1500 lines, so any further addition will fail the architecture test.
2. `DrawingSession::SetSnapSettings` doesn't clear the held target. If S or the magnet toggle is pressed and released without moving the pointer, the target is kept until the next hover. This is harmless because the pointer hasn't moved.
3. Two spec rules are not implemented yet: stronger snapping for candidates suited to the current tool (§6.1) and ranking the tool's required type first (§4.3 item 1, PRD-060). The `SnapEngine.h` header records this honestly.
4. The owner should confirm two rules the specs don't define:
   - Extension and project-to-plane rank below perpendicular/tangent and above the grid.
   - Closest-on-curve gives way to a perpendicular or tangent point on the same curve.
5. Hover cost with 0.01 mm curve sampling has not been measured against §13.
6. `geometry::CurveLiesInPlane` duplicates `app::CurveLiesOnPlane` and should be merged later.

I also saved this review to the plan file (`read-ai-prompts-claude-worker-md-and-the-calm-rabin.md`).

## Local gates

- PASS `cmake --preset windows-msvc`
- PASS `cmake --build --preset windows-msvc --parallel`
- PASS `ctest --preset windows-msvc`
- PASS `C:\Users\tak01\github\kachakachaCAD-worktrees\ui-p1-007\build-msvc2022-x64\Release\kachakacha_cad_next.exe --self-test`

## Diff stat

```text
docs/manual/README.md                              |   2 +-
 .../\344\275\234\343\202\212\346\226\271.md"       |   4 +-
 docs/v2/geometry-contract.md                       |   7 +-
 docs/v2/kcd2-format.md                             |   7 +-
 docs/v2/v1-drawing-parity.md                       |  26 +-
 samples/streamlined-railway-nose-1-87.kcd2         | Bin 73629 -> 73672 bytes
 samples/v2-sample.kcd2                             | Bin 22950 -> 22993 bytes
 src/apps/cad_next/V2MainWindow.cpp                 |   3 +-
 src/apps/cad_next/V2SelfTestInput.cpp              | 105 ++++
 src/apps/cad_next/V2SelfTestScreen.cpp             |  17 +-
 src/apps/cad_next/V2Viewport.cpp                   |   6 +-
 src/apps/cad_next/V2Viewport.h                     |   2 +
 src/apps/cad_next/V2ViewportInput.cpp              |  14 +-
 src/next/kachakacha/app/DrawingSession.cpp         |  27 +-
 src/next/kachakacha/app/DrawingSession.h           |  10 +
 src/next/kachakacha/app/Selection.cpp              |  79 +--
 src/next/kachakacha/app/Selection.h                |   4 +-
 src/next/kachakacha/geometry/CurveIntersection.cpp | 206 +++++++
 src/next/kachakacha/geometry/CurveIntersection.h   |  24 +
 src/next/kachakacha/geometry/GeometryTolerance.h   |   6 +-
 src/next/kachakacha/io/DocumentFile.cpp            |   2 +
 src/next/kachakacha/io/DocumentFileRead.cpp        |  14 +
 src/next/kachakacha/modeling/SnapEngine.cpp        | 263 ++++++---
 src/next/kachakacha/modeling/SnapEngine.h          |  82 ++-
 tests_v2/document_file_tests.cpp                   |  46 ++
 tests_v2/foundation_tests.cpp                      |   2 +
 tests_v2/grab_to_move_tests.cpp                    |   2 +-
 tests_v2/selection_tests.cpp                       | 136 ++++-
 tests_v2/session_tests.cpp                         |  84 +++
 tests_v2/snap_tests.cpp                            | 637 ++++++++++++++++++++-
 30 files changed, 1619 insertions(+), 198 deletions(-)
```
