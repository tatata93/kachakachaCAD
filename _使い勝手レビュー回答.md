# 不満点への回答(2026-09-05 批判的レビュー+市販CAD調査に基づく対応方針)

各項目: 【評価】現状の何が悪いか(批判的レビュー) →【世間】市販CADの流儀 →【対応】どう直すか。

## 全般

### 1. Escでとにかくキャンセル・無選択に

【評価】正当な指摘。現状のEscはビューポートにフォーカスがある時しか効かず、右パネルの
入力欄・面取りのペア選択・境界ピック・スライダー操作中などで効かない。「どの状態でも
Escで白紙に戻せる」という安心感がないのが使いにくさの根源。
【世間】SolidWorks/Fusionは2段階(1回目=コマンド取消、2回目=選択解除)だが、
ご指定どおり1回で「入力破棄+ツール=選択+選択解除」まで一気に戻す。
【対応】アプリ全体のイベントフィルタでEscを捕捉し、(a)進行中の入力・ピック・
プレビューを破棄 (b)ツールを選択へ (c)選択解除 (d)入力欄のフォーカスも外す、を常に実行。
テキスト入力中のEscはまず欄の編集破棄(標準動作)→同時に上記も実行。規模: 小。

### 2. 名前を後から変えられない

【評価】そのとおり不便。名前が全参照のキーなので実装を後回しにしていたが、
リネームは全CADの基本機能(ブラウザでF2)。
【対応】`Project::RenameObject(kind, 旧名, 新名)` を実装し、参照する全フィールド
(投影元/先、面の元ワイヤ、板の元面、開口、近似モデルの元・スコープ、セット、積層、
拘束、治具…)を一括更新。ツリーの右クリック「名前を変更…」+F2。グループ名も同様。
派生オブジェクト(「近似1_境界2」等)は単独リネーム不可とし、親(近似モデル)の
リネームに連動して付け替える(キーの整合を保つ)。規模: 中(機械的だが参照箇所が多い)。

## 作図モード

### 3. グリッドスナップが弱い・スナップ表示が小さい

【評価】スナップ半径が小さく、吸着マーカーが小さな薄い丸1つで、吸着したか判別できない。
【世間】AutoCAD/LibreCAD系はスナップ種別ごとに大きめのグリフ(端点=□、交点=×、
中点=△、中心=○)を色付きで出す。
【対応】スナップ半径を拡大(設定可)し、吸着時は大きい強調グリフ+吸着種別で形を変える。
吸着中はカーソル位置がグリフへ「ピタッと」動く(現在は薄丸表示のみで座標が動いていない
場面がある)。規模: 小〜中。

### 4. 端点・交点が作図点と同じ作用をしていない

【評価】重大。2点結線ツールにだけ端点・交点スナップを付けたが、直線・円弧・矩形・
移動の基準点・測定など全ツールでは効かない。場当たり実装だった。
【対応】スナップ供給を一元化: 「作図点・線の端点・交点・中点・円中心」を共通の
スナップ候補として全ツール(始点右クリック吸着含む)に配給。優先順位は
作図点=端点=交点 > 中点 > グリッド。表示は#3のグリフで種別が分かるように。規模: 中。

### 5. 正対していない3Dビューで線が引けない

【評価】作図ツールが「画面=作図面」を前提にしており、斜めから見ると使えない。
【世間】Fusion等はスケッチ平面へのレイ交差で任意視点から作図できるのが当たり前。
【対応】全作図ツールのクリック/プレビューを「カーソルのレイ×作図面の交点」に変更。
正対時は従来と同じ結果になる。スナップ(#4)も3D空間で判定。規模: 中。

### 6. 作図面以外の線を薄くする

【評価】線が重なると自分の線が見えない。妥当。
【世間】Fusionはスケッチ編集中、他ジオメトリを減光する。
【対応】「作図面にない要素を薄く表示」を追加: 作図ツール使用中は自動でON、
表示設定に常時ON/OFFトグル。作図面上(投影ワイヤ含む)だけ通常濃度。規模: 小。

### 7. C面取り・R面取りの刷新

【評価】現状は「事前に2本選んでからボタン」で、値も別の欄、平面内直線同士のみ。
離れた線の自動延長もない。ワークフローが逆で、複数ペアもできない。
【世間】AutoCADのFILLETは交差していない線を自動で延長/トリムして接続する。
InventorのフィレットはコマンドON→ペアを次々選択→半径入力→一括適用。
【対応】モーダルツール化: ①面取り/丸めボタン ②ペアを次々クリック(選んだペアは
ハイライト+プレビュー。複数ペア可) ③半径/距離を入力欄またはその場でドラッグ
④Enterで全ペアへ一括適用。離れた線は交点まで自動延長してから面取り。
3D空間の任意の(同一平面上にある)線ペアに対応し、正対不要(#5と同基盤)。規模: 中〜大。

### 8. 円弧(両端+半径)のドラッグ指定

【評価】数値入力しかなく、感覚的に膨らみを決められない。
【世間】AutoCAD/Fusionの3点円弧・両端円弧はカーソルで膨らみを追従表示して決める。
【対応】両端クリック後、カーソル移動で半径・膨らむ側をライブプレビュー、
クリック確定。数値入力は併用可(入力を始めたらそちら優先)。規模: 小。

### 9. ベジェ/スプラインの通過・制御点を作図点として残す

【評価】測定ウィンドウの「制御点に作図点」で後付けはできるが、引くとき残せる方が自然。
【対応】ツール詳細に「指定した点を作図点として残す」チェックボックス。
ONなら確定時に通過点/制御点を作図点として生成(名前は<線名>_点N)。規模: 小。

## 面・板材モード

### 10. 面の作り方の自動化・ゴードン統合

【評価】「作り方」を人が選ぶ必要はない。選択内容から一意に決まる場合がほとんど。
【対応】「面を作成」に「自動(推奨)」を既定として追加し判定を統合:
閉輪郭1つ→平面 / 断面2→ルールド / 断面3+→ロフト / 「全断面と交差する線」を
ガイドと自動判定→ゴードン(ガイド付き)。ゴードン面の区画は「面を作成」へ統合し
ツール列から1つ減らす。判定結果は実行前にラベルとプレビュー(#11)で明示、
手動で作り方を上書きも可能。規模: 中。

### 11. 選択段階での面プレビュー

【評価】結果が見えないまま実行するのは怪しい操作感。ゴースト機構(数値入力の橙破線)を
面作成にも使うべきだった。
【対応】面・板材モードで選択が変わるたび、#10の自動判定で面を試作し半透明表示。
作れない選択ならプレビュー無し+理由をパネルに表示。規模: 中。

### 12. 「厚み」機能への統合(板材化+厚み位置ワイヤ+オフセット面)

【評価】同じ「厚み方向にずらす」操作が3区画に分散している。
【対応】1つの「厚み」区画に統合: 厚み値+方向を1回設定し、出力チェックボックス
[板材を作る][厚み位置の面を作る(オフセット面)][厚み位置のワイヤを作る]。
複数チェックなら全部出力(例: ワイヤ+板材)。既存3区画はこの1区画に置き換え。規模: 中。

### 13. 面の構成線(曲率が変わる箇所)のワイヤ化を選択制に

【評価】ロフトの断面位置・曲率不連続線は工作の基準線として重要なのに出力されない。
【対応】「面を作成」に出力チェックボックス [面を作る][構成線をワイヤにする]。
構成線=各断面位置の線+接線不連続の稜線。両方/面のみ/ワイヤのみを選べ、
実行前プレビュー(#11)に構成線も薄く表示。規模: 中。

### 14. 複数の面へまたがる投影(回り込む窓)

【評価】現状は投影先が1面のみで、正面と側面へ角で回り込むパノラマ窓が作れない。
本質的な制限で、209系/キハ65のような実例で必ず詰まる。
【対応】投影先を複数面選択可にする。1つの下書き輪郭を各面へ方向投影し、
「その面に実際に載る区間」だけを切り出して面ごとの投影ワイヤに分割、
隣接面の共有縁で端点を一致させる。開口として使う場合は、面ごとの区間+共有縁で
閉じた「面ごとの開口」を自動生成(実物と同じ=角で2枚の板に分かれて窓が開く)。
派生として下書き・面の編集に追従。規模: 大(ただし最重要と認識)。

## 近似モデル

### 15. 近似ユニット(複数の近似面+形状維持対象を表で管理)

【評価】現状は「近似元1つ+スコープ」で、画像2のように近似したい面が複数あるユニットを
一括で扱えない。スコープも作成時の選択だけで、後から見えない・編集できないのは不完全。
【対応】**近似ユニット**を導入: ①周辺の線・面をまとめて選び「ユニットに取り込む」
②パネルの表に全対象が並ぶ ③各行の役割を「近似する / 形状維持(接続) / 対象外」から
任意の数選ぶ ④実行すると、近似する面それぞれが部材近似になり、形状維持対象は
「_接続」自動変形になる。出力先は「同一kcd内の新グループ(ユニット名)/新しい.kcd」を
選択。作成後もユニットの表から役割変更・再計算・追加・除外が可能。
既存の「近似元コンボ+暗黙スコープ」はこの表方式へ置き換える。規模: 大。

### 16. 部材の個別管理

【評価】全部材が1モデルの塊で、部品単位の管理(曲げ確認・名前・出力)がしにくい。
【対応】ユニットのツリーを ユニット→近似面→部材 の3階層にし、部材ごとに
名前変更(#2)・曲げ確認(その部材だけ表示は実装済み。ツリーから直接選べるように)・
型紙表示・板材化・エクスポートを右クリックで実行可能に。規模: 中(#15と同時)。

### 17. 穴の反映と後からの投影・穴あけ

【評価】(a)面に開けた開口が近似へ写らないケースがある — 開口が「面の開口」として
登録されず投影ワイヤのまま置かれている場合は写らない仕様で、これは分かりにくい。
(b)近似後に部材面へ投影・開口できないのは私が派生面を保護しすぎたため。
【対応】(a)近似作成時、近似元の面上にある「閉じた投影ワイヤ」は開口登録が無くても
自動で開口候補として検出し、写すかどうか確認を出す(既定=写す)。
(b)部材面(派生面)への投影・開口を解禁。開口はモデルに部材番号付きで記録し、
再生成時に再投影して追従。型紙・実体化にも反映。規模: 中。

### 18. 近似面の板材化(追従/固定+出力選択)

【評価】現状は固定の板しか作れず、出力の種類も選べない。
【対応】#12の「厚み」機能と同じUIで [板材][面][ワイヤ] を選べるようにし、さらに
「近似の再計算に追従する(派生)/現在の形で固定(独立)」を選択制に。追従板は
近似再計算で作り直し、固定板は通常オブジェクトとしてそのまま残す。規模: 中。

## 提案する実装順

1. **即効の使い勝手**: #1 Esc / #3 スナップ強化 / #4 端点・交点の一元化 / #8 円弧ドラッグ /
   #9 スプライン点 / #6 減光 (作図の日常操作を先に直す)
2. **作図の大物**: #5 3Dビュー作図 / #7 面取り刷新 / #2 リネーム
3. **面・板材**: #10+#11 自動判定+プレビュー / #12 厚み統合 / #13 構成線
4. **回り込み投影 #14**(単体で大きい)
5. **近似ユニット #15+#16+#17+#18**(仕様が固まっている今のうちに続けて)
